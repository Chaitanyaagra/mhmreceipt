// Test stub for app-common's `import { db } from './firebase-config.js'`.
// The real module initialises the Firebase SDK (browser + network); tests only
// exercise the pure logic, so an empty db object is enough.
export const db = {};
