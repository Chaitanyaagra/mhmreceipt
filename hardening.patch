diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/.github/workflows/deploy.yml MHMRWS-Portal/.github/workflows/deploy.yml
--- a/MHMRWS-Portal/.github/workflows/deploy.yml	2026-07-22 09:58:18.000000000 +0000
+++ MHMRWS-Portal/.github/workflows/deploy.yml	2026-07-24 11:57:01.462017139 +0000
@@ -29,10 +29,20 @@
       - name: Configure GitHub Pages
         uses: actions/configure-pages@v5
 
+      # Build the publishable site in a clean folder rather than shipping the
+      # whole repo. The security rules in particular have no business being on
+      # the public site: they are a readable map of exactly what each committee
+      # role may do and where the soft spots are. The setup guides are internal
+      # documents and belong in the repo, not on the site.
+      - name: Stage publishable files
+        run: |
+          mkdir -p _site
+          rsync -av             --exclude '_site'             --exclude '.git*'             --exclude '*.md'             --exclude '*.rules'             --exclude 'firebase.json'             --exclude '.firebaserc'             ./ _site/
+
       - name: Upload site as Pages artifact
         uses: actions/upload-pages-artifact@v3
         with:
-          path: '.'
+          path: '_site'
 
       - name: Deploy to GitHub Pages
         id: deployment
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/MIGRATION.md MHMRWS-Portal/MIGRATION.md
--- a/MHMRWS-Portal/MIGRATION.md	1970-01-01 00:00:00.000000000 +0000
+++ MHMRWS-Portal/MIGRATION.md	2026-07-24 12:00:35.951415822 +0000
@@ -0,0 +1,132 @@
+# MIGRATION — security hardening release
+
+Read this before deploying. Most of it is "deploy in this order"; there is one
+optional script at the end.
+
+---
+
+## 1. Deploy order matters
+
+**Deploy the site first, then the rules.** Not the other way round.
+
+The new rules require a `publicToken` on any payment being marked verified, and
+a verified email address before a resident can file a payment. Only the new
+code writes those. If the rules go up first, a treasurer with the old page
+still cached gets a permission error in the middle of issuing a receipt.
+
+```
+1. git push            → GitHub Pages redeploys index/admin/verify + JS
+2. Hard-refresh the admin panel once (Ctrl-Shift-R) so the new
+   service worker takes over — it is network-first, so this is quick.
+3. Firebase Console → Firestore → Rules → paste firestore.rules → Publish
+4. Firebase Console → Storage   → Rules → paste storage.rules   → Publish
+```
+
+Paste the rules into the Console editor rather than deploying blind: it
+compiles as you type and will point at the exact line if anything is wrong.
+
+The rules ship as separate files and are **no longer published to the live
+site** (see `.github/workflows/deploy.yml`), so you have to copy them from the
+repo, not from the URL.
+
+---
+
+## 2. What will change for residents, immediately
+
+**Residents who have not verified their email cannot file a payment.** That is
+the point of the change, but it will generate questions, so it is worth a
+WhatsApp message to the group before you deploy.
+
+The dashboard now shows a banner with a "Link dobara bhejein" (resend) button
+for anyone in that position, so it is self-service — nobody has to come to the
+office. New registrations get the verification mail automatically.
+
+If a resident says the mail never arrived: Firebase Console → Authentication →
+Templates → Email address verification. Check the sender domain and that the
+template is enabled. The mail very often lands in spam on first send.
+
+---
+
+## 3. One flat, one member
+
+Approval now writes a `flatClaims/{tower}_{flat}` document, and refuses if that
+flat is already claimed. Rejecting or deactivating a member releases it.
+
+**This is not applied retroactively.** Members approved before this release
+have no claim document, so the first person approved for any given flat after
+the upgrade will succeed even if a duplicate already exists. If you want the
+existing duplicates found, sort the members table by flat and look — with 454
+flats that is a ten-minute job and more reliable than a script guessing which
+of two records is the real one.
+
+---
+
+## 4. Public verification tokens — the enumeration fix
+
+Receipt and card QR codes used to carry the sequential number, and the public
+collections were keyed by it. Anyone could request `MHM-2026-000001`,
+`000002`, `000003` … and collect the whole resident directory, or walk the
+receipt numbers and total up the society's income.
+
+New receipts and cards carry a 128-bit random token instead. `verify.html`
+still accepts the old `?receipt=` and `?member=` links, **so every receipt and
+card already in a resident's hands keeps working.**
+
+That backwards compatibility is also the catch: the old documents are still
+keyed by the sequential number, so **records issued before this release remain
+enumerable** until you migrate them.
+
+### Deciding whether to migrate
+
+| | Keep legacy docs | Purge them |
+|---|---|---|
+| Old printed receipts / cards | keep verifying | stop verifying |
+| Old records enumerable | yes | no |
+
+The sensible sequence for most societies:
+
+1. Run the migration **without** `--purge-legacy` now. Every member and
+   payment gets a token, so all *newly downloaded* cards and receipts are safe.
+2. Ask residents to re-download their membership card (one line in the
+   WhatsApp group — the button is on their dashboard).
+3. A month later, run it again **with** `--purge-legacy`.
+
+### Running it
+
+The script needs a service account key, which bypasses security rules — so
+treat that file like a password, and delete it when you are done.
+
+```bash
+# Firebase Console → Project settings → Service accounts
+#   → "Generate new private key" → save as serviceAccountKey.json
+
+cd tools
+npm install firebase-admin
+node migrate-public-tokens.mjs --dry-run          # shows what it would do
+node migrate-public-tokens.mjs                    # backfill tokens
+node migrate-public-tokens.mjs --purge-legacy     # later: remove old keys
+
+rm ../serviceAccountKey.json
+```
+
+**Take a backup first** — Admin Panel → Import/Export → Download Backup.
+
+---
+
+## 5. Turn on App Check
+
+Not done for you, because it needs a key only you can create. It is the single
+biggest remaining improvement and it is free. Instructions are in the comment
+block in `firebase-config.js`. Until you do it, anyone can talk to your
+Firebase project directly from a script — the rules will refuse them, but you
+pay for every refused request.
+
+---
+
+## 6. After deploying, check the console once
+
+The new Content-Security-Policy is deliberately tight. Open the site, press
+F12, and look at the Console tab for any `Refused to ...` messages. Anything
+legitimate that the policy blocks will say so there, and the fix is to add that
+domain to the matching directive in the `<meta http-equiv>` block at the top of
+the page. Check all three pages: `index.html`, `admin.html`, `verify.html`.
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/SETUP-GUIDE.md MHMRWS-Portal/SETUP-GUIDE.md
--- a/MHMRWS-Portal/SETUP-GUIDE.md	2026-07-23 11:28:38.000000000 +0000
+++ MHMRWS-Portal/SETUP-GUIDE.md	2026-07-25 13:05:02.822145322 +0000
@@ -114,6 +114,22 @@
 
 ---
 
+## ⚠️ Pehle yeh padhein — Security Hardening Release
+
+Agar aapka portal pehle se live hai, to seedha yahan se shuru **na** karein.
+`MIGRATION.md` pehle padhein: usme deploy ka sahi order, email-verification ka
+naya rule, aur purane receipts/cards ke liye migration script sab likha hai.
+
+Naya install kar rahe hain? To yeh guide theek hai — bas do cheezein extra:
+
+* **Step 2 ke baad:** `firebase-config.js` mein App Check ka comment block
+  padhein aur reCAPTCHA site key daal dein. Free hai, aur bina iske koi bhi
+  script aapke Firebase project se seedha baat kar sakta hai.
+* **Step 7 (Test) mein:** register karke dekhein ki verification email aata
+  hai ya nahi. Ab bina email verify kiye payment submit nahi hoga.
+
+---
+
 ## Step 3 — Security Rules Deploy Karein
 
 **Option A — Firebase Console se (sabse aasaan):**
@@ -235,7 +251,7 @@
 ## 🎨 Customization
 
 - Colors: `styles.css` ke top mein `:root { --navy-950: ...; --saffron-500: ...; }` — yahin se poori site ka theme change ho jaata hai.
-- Logo: seal `logo-data.js` mein base64 ke roop mein embedded hai (isliye woh kabhi missing nahi ho sakta). Badalna ho toh naya logo bhej kar dobara generate karwa lein. `icon-192.png` / `icon-512.png` PWA ke liye alag files hain.
+- Logo: seal ab `logo.webp` file mein hai (pehle 66 KB base64 JS mein tha — woh har page load par bhaari padta tha). Badalna ho toh usi naam se naya `logo.webp` daal dein. PDF/card ke liye woh apne aap data-URI bana leta hai. `icon-192.png` / `icon-512.png` PWA ke liye alag files hain.
 - Society details (address, phone, bank details): yeh sab database mein hain, code mein nahi — Admin Panel → Settings se hi update karein.
 
 ## 🆘 Common Errors
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/admin.html MHMRWS-Portal/admin.html
--- a/MHMRWS-Portal/admin.html	2026-07-23 11:26:32.000000000 +0000
+++ MHMRWS-Portal/admin.html	2026-07-25 12:53:28.451645720 +0000
@@ -2,16 +2,67 @@
 <html lang="en">
 <head>
 <meta charset="UTF-8">
+<!-- ==========================================================================
+     CONTENT SECURITY POLICY
+     GitHub Pages cannot set HTTP headers, so this goes in a meta tag — which
+     must stay above every script and stylesheet it governs.
+
+     What this buys: an injected <script src="https://evil.example/x.js"> will
+     not load, and stolen data cannot be POSTed anywhere except the endpoints
+     listed in connect-src. That is the realistic attack against a page that
+     handles Aadhaar uploads and payment records.
+
+     What it does NOT buy: script-src still has 'unsafe-inline', because every
+     page here is built from inline module scripts and inline style attributes. Removing that needs the inline scripts moved into their
+     own .js files first — worth doing, but it is a refactor, not a patch.
+
+     frame-ancestors is not supported in a meta tag, so this cannot stop the
+     site being framed. If you ever move off GitHub Pages, set
+     `X-Frame-Options: DENY` (or frame-ancestors 'none') as a real header.
+
+     AFTER DEPLOYING: open the site, press F12, and check the Console for
+     "Refused to ..." messages. Any legitimate endpoint this policy blocks will
+     announce itself there and just needs adding to the right directive.
+     ========================================================================== -->
+<meta http-equiv="Content-Security-Policy" content="
+  default-src 'self';
+  script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://www.gstatic.com https://accounts.google.com https://apis.google.com https://www.google.com;
+  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
+  img-src 'self' data: blob: https://firebasestorage.googleapis.com https://*.googleusercontent.com;
+  font-src 'self' data: https://fonts.gstatic.com;
+  connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://accounts.google.com;
+  frame-src https://www.google.com https://accounts.google.com;
+  worker-src 'self';
+  object-src 'none';
+  base-uri 'self';
+  form-action 'self';
+">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Admin Panel — MHMRWS Digital Portal</title>
 <meta name="robots" content="noindex">
 <link rel="icon" href="favicon-32.png">
+<meta name="color-scheme" content="light">
+<link rel="preconnect" href="https://fonts.googleapis.com">
+<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link rel="stylesheet" href="styles.css">
-<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.2/jspdf.umd.min.js"></script>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcode/1.5.3/qrcode.min.js"></script>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.4/chart.umd.min.js"></script>
+<!-- Third-party libraries.
+     These are loaded from a CDN with no integrity hash, which means a
+     compromised or hijacked CDN gets script execution on a page that handles
+     ID documents and payment records. Fix this — it takes two minutes:
+
+       1. Open https://cdnjs.com and search for the library.
+       2. Click the copy button next to "SRI" for the exact version below.
+       3. Paste the sha384-... value into an integrity="" attribute here.
+
+     Stronger still: download these five files into a vendor/ folder in this
+     repo and load them from './vendor/...'. Same-origin scripts need no hash,
+     cannot be tampered with by a third party, and let the service worker cache
+     them for genuine offline use. -->
+<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.2/jspdf.umd.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcode/1.5.3/qrcode.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+<script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.4/chart.umd.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
 <style>
   body{ background:var(--paper); }
   .login-screen{ min-height:100vh; display:grid; place-items:center; background:var(--gradient-navy); padding:20px; }
@@ -35,7 +86,24 @@
 
   .main-area{ padding:28px 32px 60px; max-width:1360px; width:100%; }
   .topbar{ display:flex; justify-content:space-between; align-items:center; margin-bottom:26px; flex-wrap:wrap; gap:14px; }
-  .view-title{ font-size:23px; }
+  .view-title{ font-size:23px; position:relative; padding-top:14px; }
+  /* The engraved gold rule from the resident portal, so the admin panel reads
+     as the same product. Pure CSS — no markup change across the 9 views. */
+  .view-title::before{
+    content:''; position:absolute; top:0; left:0; width:44px; height:2px; border-radius:2px;
+    background:var(--gradient-saffron);
+  }
+  .view-title::after{
+    content:''; position:absolute; top:0; left:50px; width:14px; height:2px; border-radius:2px;
+    background:var(--magenta-500);
+  }
+  /* Sidebar brand gets the same hairline the site header has. */
+  .sidebar-brand{ position:relative; }
+  .sidebar-brand::after{
+    content:''; position:absolute; left:8px; right:8px; bottom:12px; height:1px;
+    background:linear-gradient(90deg, var(--gold-500), var(--magenta-500) 55%, transparent);
+    opacity:.55;
+  }
   .fy-select{ width:auto; }
 
   .stat-cards{ display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-bottom:22px; }
@@ -157,7 +225,7 @@
       <button data-view="importExport"><span class="icon"><svg viewBox="0 0 24 24"><use href="#a-importExport"/></svg></span> Import / Export</button>
       <button data-view="settings"><span class="icon"><svg viewBox="0 0 24 24"><use href="#a-settings"/></svg></span> Settings &amp; Backup</button>
       <button data-view="adminUsers"><span class="icon"><svg viewBox="0 0 24 24"><use href="#a-adminUsers"/></svg></span> Admin Users</button>
-      <button data-view="auditLog"><span class="icon"><svg viewBox="0 0 24 24"><use href="#a-auditLog"/></svg></span> Audit Log</button>
+      <button data-view="auditLog"><span class="icon"><svg viewBox="0 0 24 24"><use href="#a-auditLog"/></svg></span> Activity Log</button>
     </nav>
     <div class="sidebar-foot">
       <div class="admin-chip"><b id="adminName">—</b><span id="adminRole">—</span></div>
@@ -225,6 +293,7 @@
           <tbody id="membersTableBody"><tr><td colspan="6" class="t-muted">Loading…</td></tr></tbody>
         </table>
       </div>
+      <div class="pager" id="membersPager"></div>
     </div>
 
     <!-- ---------------- PAYMENTS ---------------- -->
@@ -248,6 +317,7 @@
           <table><thead><tr><th>Resident</th><th>Flat</th><th>Amount</th><th>Mode</th><th>Status</th><th>Receipt No.</th><th>Date</th></tr></thead>
           <tbody id="allPaymentsBody"><tr><td colspan="7" class="t-muted">Loading…</td></tr></tbody></table>
         </div>
+        <div class="pager" id="allPaymentsPager"></div>
       </div>
       <div id="paymentsDefaultersPanel" style="display:none;">
         <div class="toolbar"><span id="defaultersTotal" class="mono" style="font-size:12.5px; color:var(--ink-600);"></span></div>
@@ -467,12 +537,16 @@
       </div>
     </div>
 
-    <!-- ---------------- AUDIT LOG ---------------- -->
+    <!-- ---------------- ACTIVITY LOG ---------------- -->
     <div class="view-panel" id="view-auditLog">
-      <div class="topbar"><h2 class="view-title">Audit Log</h2></div>
+      <div class="topbar"><h2 class="view-title">Activity Log</h2></div>
+      <p class="t-sub" style="font-size:12.5px; margin:-6px 0 14px;">
+        Committee ke actions ka record. Yeh browser se likha jaata hai, isliye ise
+        reference ke liye istemal karein — kisi vivaad mein proof ke taur par nahi.
+      </p>
       <div class="table-wrap">
-        <table><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Details</th><th>IP</th></tr></thead>
-        <tbody id="auditLogBody"><tr><td colspan="5" class="t-muted">Loading…</td></tr></tbody></table>
+        <table><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Details</th></tr></thead>
+        <tbody id="auditLogBody"><tr><td colspan="4" class="t-muted">Loading…</td></tr></tbody></table>
       </div>
     </div>
 
@@ -612,7 +686,8 @@
   getAuth, signInWithEmailAndPassword, onAuthStateChanged, signOut, createUserWithEmailAndPassword
 } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
 import {
-  doc, getDoc, setDoc, updateDoc, deleteDoc, addDoc, collection, getDocs, query, where, orderBy, serverTimestamp, limit
+  doc, getDoc, setDoc, updateDoc, deleteDoc, addDoc, collection, getDocs, query, where, orderBy, serverTimestamp, limit,
+  writeBatch, runTransaction
 } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
 import {
   ref as storageRef, uploadBytes, getDownloadURL
@@ -620,10 +695,12 @@
 import {
   showToast, escapeHtml, fmtDate, fmtDateTime, formatINR, currentFinancialYear, financialYearOptions,
   generateMemberId, generateReceiptNumber, logAudit, generateReceiptPDF, exportToExcel, parseExcelFile,
-  encryptFile, decryptToBlob, backupToDrive, waLink, serializeForExport,
+  encryptFile, decryptToBlob, backupToDrive, waLink, waShareText, serializeForExport,
+  newPublicToken, publicKeyForMember,
   TOWER_IDS, TOWER_PLAN, flatsForTower, isValidFlat,
   duesFor, DUES_LABEL, DUES_BADGE,
-  EXPENSE_CATEGORIES, EXPENSE_MODES, expenseSummary, fundPosition, validateExpense
+  EXPENSE_CATEGORIES, EXPENSE_MODES, expenseSummary, fundPosition, validateExpense,
+  renderPager, skeletonTableRows, emptyState
 } from './app-common.js';
 
 /* ---------------------------------------------------------------------- */
@@ -998,9 +1075,25 @@
   inactive: '<span class="badge badge-neutral">Inactive</span>'
 };
 
+const MEMBERS_PAGE_SIZE = 25;
+let membersPage = 1;
 function renderMembersTable() {
   const rows = filteredMembers();
-  document.getElementById('membersTableBody').innerHTML = rows.map(m => `
+  const body = document.getElementById('membersTableBody');
+  const pager = document.getElementById('membersPager');
+
+  if (!rows.length) {
+    body.innerHTML = `<tr><td colspan="6">${emptyState('Koi member nahi mila', 'In filters ke saath koi resident nahi hai.', { compact: true })}</td></tr>`;
+    if (pager) pager.innerHTML = '';
+    return;
+  }
+
+  // Clamp the page if the filtered set shrank under the current page.
+  const pages = Math.max(1, Math.ceil(rows.length / MEMBERS_PAGE_SIZE));
+  if (membersPage > pages) membersPage = pages;
+  const slice = rows.slice((membersPage - 1) * MEMBERS_PAGE_SIZE, membersPage * MEMBERS_PAGE_SIZE);
+
+  body.innerHTML = slice.map(m => `
     <tr>
       <td data-label="Resident">${escapeHtml(m.name)}</td>
       <td data-label="Flat / Tower">${escapeHtml(m.flatNumber)} / ${escapeHtml(m.tower)}</td>
@@ -1009,10 +1102,18 @@
       <td data-label="Member ID" class="mono">${escapeHtml(m.memberID || '—')}</td>
       <td data-label="Actions"><button class="btn btn-outline btn-sm" data-view-member="${m.id}">View Details</button></td>
     </tr>
-  `).join('') || '<tr><td colspan="6" class="t-muted">No members match these filters.</td></tr>';
+  `).join('');
   document.querySelectorAll('[data-view-member]').forEach(b => b.addEventListener('click', () => openMemberModal(b.dataset.viewMember)));
+
+  renderPager(pager, {
+    page: membersPage, pageSize: MEMBERS_PAGE_SIZE, total: rows.length,
+    onPage: (p) => { membersPage = p; renderMembersTable();
+      document.getElementById('membersTableBody').scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }
+  });
 }
-['memberSearch', 'filterTower', 'filterStatus'].forEach(id => document.getElementById(id).addEventListener('input', renderMembersTable));
+// A filter change should always return to page 1, else you can land on an empty page.
+['memberSearch', 'filterTower', 'filterStatus'].forEach(id =>
+  document.getElementById(id).addEventListener('input', () => { membersPage = 1; renderMembersTable(); }));
 
 document.getElementById('exportMembersBtn').addEventListener('click', () => {
   exportToExcel(serializeForExport(filteredMembers()).map(m => ({
@@ -1070,16 +1171,64 @@
   } catch (e) { showToast('Decrypt fail — passphrase galat ho sakta hai.', 'error'); }
 }
 
+/* One flat, one member. Firestore has no unique index, so uniqueness is bought
+   with a document ID: flatClaims/A_101 either exists or it does not. */
+function flatClaimId(tower, flatNumber) {
+  return `${String(tower || '').trim()}_${String(flatNumber || '').trim()}`;
+}
+
+/* Approval used to be three separate round trips — assign an ID, update the
+   member, write the public record — with no guarantee the later ones ran. A
+   half-approved resident ends up holding a card whose QR resolves to "not
+   found", which is exactly the moment the portal has to be trustworthy.
+   All of it now commits together, or none of it does. */
 async function approveMember(id) {
   const m = allMembers.find(x => x.id === id) || {};
+  const claimRef = doc(db, 'flatClaims', flatClaimId(m.tower, m.flatNumber));
+
+  // Cheap pre-check so the common case gives a readable message instead of a
+  // transaction abort. The transaction below is what actually enforces it.
+  const existing = await getDoc(claimRef);
+  if (existing.exists() && existing.data().memberDocId !== id) {
+    const holder = allMembers.find(x => x.id === existing.data().memberDocId);
+    showToast(`Tower ${m.tower}, Flat ${m.flatNumber} pehle se ${holder?.name || 'kisi aur member'} ke naam par hai. Pehle unhe deactivate karein.`, 'error');
+    return;
+  }
+
   const memberID = await generateMemberId();
-  await updateDoc(doc(db, 'members', id), { status: 'approved', memberID, approvedBy: currentAdmin.email, approvedAt: serverTimestamp(), updatedAt: serverTimestamp() });
-  // Public record behind the membership card's QR code. Deliberately holds
-  // only what a guard at the gate needs — no contact details, no documents.
-  await setDoc(doc(db, 'membersPublic', memberID), {
-    memberID, name: m.name || '', tower: m.tower || '', flatNumber: m.flatNumber || '',
-    residentType: m.residentType || '', status: 'approved', updatedAt: serverTimestamp()
-  });
+  const publicToken = newPublicToken();
+
+  try {
+    await runTransaction(db, async (tx) => {
+      const claim = await tx.get(claimRef);
+      if (claim.exists() && claim.data().memberDocId !== id) throw new Error('FLAT_TAKEN');
+      tx.update(doc(db, 'members', id), {
+        status: 'approved', memberID, publicToken,
+        approvedBy: currentAdmin.email, approvedAt: serverTimestamp(), updatedAt: serverTimestamp()
+      });
+      // Public record behind the membership card's QR code. Keyed by the random
+      // token, not the member number, so the directory cannot be enumerated.
+      // Holds only what a guard at the gate needs — no contact details, no
+      // documents, no payment history.
+      tx.set(doc(db, 'membersPublic', publicToken), {
+        memberID, name: m.name || '', tower: m.tower || '', flatNumber: m.flatNumber || '',
+        residentType: m.residentType || '', status: 'approved', updatedAt: serverTimestamp()
+      });
+      tx.set(claimRef, {
+        tower: m.tower || '', flatNumber: m.flatNumber || '',
+        memberDocId: id, memberUid: m.uid || null, memberID, claimedAt: serverTimestamp()
+      });
+    });
+  } catch (err) {
+    if (err.message === 'FLAT_TAKEN') {
+      showToast('Yeh flat abhi-abhi kisi aur ko allot ho gaya. Page refresh karke dobara dekhein.', 'error');
+    } else {
+      console.error(err);
+      showToast('Approve nahi ho paaya — kuch bhi save nahi hua. Dobara try karein.', 'error');
+    }
+    return;
+  }
+
   await logAudit(auth.currentUser, 'approve_member', { id, memberID });
   showToast(`Approved! Member ID: ${memberID}`, 'success');
   document.getElementById('memberModalBackdrop').classList.remove('open');
@@ -1089,10 +1238,13 @@
   const reason = prompt('Reject karne ki wajah:');
   if (reason === null) return;
   const m = allMembers.find(x => x.id === id) || {};
-  await updateDoc(doc(db, 'members', id), { status: 'rejected', rejectionReason: reason, updatedAt: serverTimestamp() });
-  if (m.memberID) {
-    await setDoc(doc(db, 'membersPublic', m.memberID), { status: 'rejected', updatedAt: serverTimestamp() }, { merge: true });
-  }
+  const batch = writeBatch(db);
+  batch.update(doc(db, 'members', id), { status: 'rejected', rejectionReason: reason, updatedAt: serverTimestamp() });
+  const rejKey = publicKeyForMember(m);
+  if (rejKey) batch.set(doc(db, 'membersPublic', rejKey), { status: 'rejected', updatedAt: serverTimestamp() }, { merge: true });
+  // Free the flat so whoever actually lives there can be approved for it.
+  if (m.tower && m.flatNumber) batch.delete(doc(db, 'flatClaims', flatClaimId(m.tower, m.flatNumber)));
+  await batch.commit();
   await logAudit(auth.currentUser, 'reject_member', { id, reason });
   showToast('Registration reject kar diya gaya.', 'success');
   document.getElementById('memberModalBackdrop').classList.remove('open');
@@ -1102,12 +1254,16 @@
   const reason = prompt('Deactivate karne ki wajah (history surakshit rahegi):');
   if (reason === null) return;
   const m = allMembers.find(x => x.id === id) || {};
-  await updateDoc(doc(db, 'members', id), { status: 'inactive', deactivatedReason: reason, deactivatedAt: serverTimestamp(), updatedAt: serverTimestamp() });
+  const batch = writeBatch(db);
+  batch.update(doc(db, 'members', id), { status: 'inactive', deactivatedReason: reason, deactivatedAt: serverTimestamp(), updatedAt: serverTimestamp() });
   // Flip the public record too, so the card stops verifying at the gate the
-  // moment the member is deactivated.
-  if (m.memberID) {
-    await setDoc(doc(db, 'membersPublic', m.memberID), { status: 'inactive', updatedAt: serverTimestamp() }, { merge: true });
-  }
+  // moment the member is deactivated — in the same commit, so it can never be
+  // the case that the member is inactive but the card still scans as valid.
+  const deacKey = publicKeyForMember(m);
+  if (deacKey) batch.set(doc(db, 'membersPublic', deacKey), { status: 'inactive', updatedAt: serverTimestamp() }, { merge: true });
+  // Release the flat for the next resident.
+  if (m.tower && m.flatNumber) batch.delete(doc(db, 'flatClaims', flatClaimId(m.tower, m.flatNumber)));
+  await batch.commit();
   await logAudit(auth.currentUser, 'deactivate_member', { id, reason });
   showToast('Member deactivate ho gaya.', 'success');
   document.getElementById('memberModalBackdrop').classList.remove('open');
@@ -1141,17 +1297,38 @@
   document.querySelectorAll('[data-pay-detail]').forEach(b => b.addEventListener('click', () => openPaymentDetail(b.dataset.payDetail)));
 }
 
+const PAYMENTS_PAGE_SIZE = 25;
+let allPaymentsPage = 1;
 function renderAllPayments() {
   const rows = [...allPayments].sort((a, b) => (b.submittedAt?.seconds || 0) - (a.submittedAt?.seconds || 0));
   const badge = { pending_approval: 'badge-warning', pending_verification: 'badge-warning', verified: 'badge-success', rejected: 'badge-danger' };
-  document.getElementById('allPaymentsBody').innerHTML = rows.map(p => `
+  const body = document.getElementById('allPaymentsBody');
+  const pager = document.getElementById('allPaymentsPager');
+
+  if (!rows.length) {
+    body.innerHTML = `<tr><td colspan="7">${emptyState('Abhi tak koi payment nahi', 'Verify ki gayi aur pending — sabhi payments yahan aayenge.', { compact: true })}</td></tr>`;
+    if (pager) pager.innerHTML = '';
+    return;
+  }
+
+  const pages = Math.max(1, Math.ceil(rows.length / PAYMENTS_PAGE_SIZE));
+  if (allPaymentsPage > pages) allPaymentsPage = pages;
+  const slice = rows.slice((allPaymentsPage - 1) * PAYMENTS_PAGE_SIZE, allPaymentsPage * PAYMENTS_PAGE_SIZE);
+
+  body.innerHTML = slice.map(p => `
     <tr>
       <td data-label="Resident">${escapeHtml(p.residentName)}</td><td data-label="Flat">${escapeHtml(p.flatNumber)}</td><td data-label="Amount"><b>${formatINR(p.amount)}</b></td>
       <td data-label="Mode">${escapeHtml((p.mode || '').toUpperCase())}</td>
       <td data-label="Status"><span class="badge ${badge[p.status] || 'badge-neutral'}">${escapeHtml((p.status || '').replace('_', ' '))}</span></td>
       <td data-label="Receipt No." class="mono">${escapeHtml(p.receiptNumber || '—')}</td><td data-label="Date">${fmtDate(p.submittedAt)}</td>
     </tr>
-  `).join('') || '<tr><td colspan="7" class="t-muted">No payments yet.</td></tr>';
+  `).join('');
+
+  renderPager(pager, {
+    page: allPaymentsPage, pageSize: PAYMENTS_PAGE_SIZE, total: rows.length,
+    onPage: (p) => { allPaymentsPage = p; renderAllPayments();
+      document.getElementById('allPaymentsBody').scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }
+  });
 }
 
 document.getElementById('exportPaymentsBtn').addEventListener('click', () => {
@@ -1188,18 +1365,48 @@
 }
 document.getElementById('closePaymentDetailModal').addEventListener('click', () => document.getElementById('paymentDetailModalBackdrop').classList.remove('open'));
 
+/* Verification was four independent round trips: bump the counter, update the
+   payment, write the public receipt, log it. Lose the connection between any
+   two and the society is left with a payment marked verified whose printed QR
+   resolves to "receipt not found" — or a burned receipt number and no receipt.
+   The payment record and its public counterpart now commit as one write.
+
+   (The counter still has to be bumped first, because Firestore cannot batch a
+   transaction with a batch. A crash in that gap costs a gap in the receipt
+   sequence, which is inert; the case that actually hurt — a verified payment
+   with no verifiable receipt — is now impossible.) */
 async function verifyPayment(id) {
   const p = allPayments.find(x => x.id === id);
-  const receiptNumber = await generateReceiptNumber(p.financialYear);
-  await updateDoc(doc(db, 'payments', id), { status: 'verified', receiptNumber, verifiedAt: serverTimestamp(), verifiedBy: currentAdmin.email });
-  await setDoc(doc(db, 'receiptsPublic', receiptNumber), {
-    receiptNumber, residentName: p.residentName, flatNumber: p.flatNumber, tower: p.tower,
-    amount: p.amount, financialYear: p.financialYear, verifiedAt: serverTimestamp(), status: 'Verified'
-  });
-  await logAudit(auth.currentUser, 'verify_payment', { paymentId: id, receiptNumber, amount: p.amount });
-  showToast(`Verified! Receipt: ${receiptNumber}`, 'success');
-  document.getElementById('paymentDetailModalBackdrop').classList.remove('open');
-  await reloadPayments(); renderPendingPayments(); renderAllPayments(); renderOverview(); renderDefaulters();
+  if (!p) return;
+  const btn = document.getElementById('verifyBtn');
+  if (btn) { btn.disabled = true; btn.textContent = 'Verifying…'; }
+  try {
+    const receiptNumber = await generateReceiptNumber(p.financialYear);
+    const publicToken = newPublicToken();
+
+    const batch = writeBatch(db);
+    batch.update(doc(db, 'payments', id), {
+      status: 'verified', receiptNumber, publicToken,
+      verifiedAt: serverTimestamp(), verifiedBy: currentAdmin.email
+    });
+    // Keyed by the random token: a resident scanning their own QR resolves it
+    // instantly, and nobody can walk the receipt numbers to total up what the
+    // society has collected and from whom.
+    batch.set(doc(db, 'receiptsPublic', publicToken), {
+      receiptNumber, residentName: p.residentName, flatNumber: p.flatNumber, tower: p.tower,
+      amount: p.amount, financialYear: p.financialYear, verifiedAt: serverTimestamp(), status: 'Verified'
+    });
+    await batch.commit();
+
+    await logAudit(auth.currentUser, 'verify_payment', { paymentId: id, receiptNumber, amount: p.amount });
+    showToast(`Verified! Receipt: ${receiptNumber}`, 'success');
+    document.getElementById('paymentDetailModalBackdrop').classList.remove('open');
+    await reloadPayments(); renderPendingPayments(); renderAllPayments(); renderOverview(); renderDefaulters();
+  } catch (e) {
+    console.error(e);
+    showToast('Verify nahi ho paaya — payment jaisa tha waisa hi hai. Dobara try karein.', 'error');
+    if (btn) { btn.disabled = false; btn.textContent = '\u2713 Verify & Issue Receipt'; }
+  }
 }
 async function rejectPayment(id) {
   const reason = prompt('Reject karne ki wajah:');
@@ -1224,6 +1431,27 @@
     .sort((a, b) => b.d.outstanding - a.d.outstanding);   // biggest debts first
 }
 
+/* A maintenance reminder most residents will actually read: Hindi first, then
+   the same in English, with the exact figures and the flat it concerns. Kept
+   as one builder so the per-defaulter button and any future bulk flow stay in
+   step. */
+function defaulterReminderText(m, d) {
+  const flat = `${m.tower}-${m.flatNumber}`;
+  return [
+    `Namaste ${m.name} ji,`,
+    ``,
+    `Max Heights Majestic RWS ki taraf se maintenance reminder:`,
+    `Flat ${flat} par FY ${selectedFY} ka ${formatINR(d.outstanding)} bakaya hai (kul ${formatINR(d.expected)}).`,
+    `Kripya resident portal se online ya office mein jama kar dein.`,
+    ``,
+    `— — —`,
+    `Reminder: ${formatINR(d.outstanding)} of your FY ${selectedFY} maintenance for Flat ${flat} is outstanding. Please pay via the resident portal or at the office.`,
+    ``,
+    `Dhanyavaad,`,
+    `MHMRWS Committee`
+  ].join('\n');
+}
+
 function renderDefaulters() {
   const rows = outstandingMembers();
   const body = document.getElementById('defaultersBody');
@@ -1253,7 +1481,7 @@
         <span class="t-muted" style="font-size:11.5px;"> of ${formatINR(d.expected)}</span></td>
       <td data-label="Status"><span class="badge ${DUES_BADGE[d.status]}">${escapeHtml(DUES_LABEL[d.status])}</span></td>
       <td data-label="Remind"><a class="btn btn-outline btn-sm" target="_blank" rel="noopener"
-        href="${waLink(m.mobile, `Dear ${m.name}, MHMRWS reminder: ${formatINR(d.outstanding)} of your FY ${selectedFY} maintenance (${formatINR(d.expected)}) is still outstanding. You can pay via the resident portal.`)}">WhatsApp</a></td>
+        href="${waLink(m.mobile, defaulterReminderText(m, d))}">WhatsApp</a></td>
     </tr>
   `).join('') || '<tr><td colspan="5" class="t-muted">No outstanding dues for this FY</td></tr>';
 }
@@ -1392,6 +1620,17 @@
   await logAudit(auth.currentUser, 'add_notice', { title: f.titleEn.value });
   showToast('Notice post ho gaya.', 'success');
   document.getElementById('noticeModalBackdrop').classList.remove('open');
+
+  // A notice nobody sees isn't much use, so offer to push it to the society
+  // WhatsApp group straight away. Hindi if we have it, else English.
+  const shareTitle = f.titleHi.value || f.titleEn.value;
+  const shareBody = f.bodyHi.value || f.bodyEn.value;
+  const shareLines = [`📢 ${shareTitle}`, '', shareBody, '', '— Max Heights Majestic RWS'];
+  if (attachmentURL) shareLines.push('', attachmentURL);
+  if (confirm('Notice post ho gaya. Ise WhatsApp group par share karein?')) {
+    window.open(waShareText(shareLines.join('\n')), '_blank', 'noopener');
+  }
+
   f.reset(); loadContentPanels();
 });
 document.getElementById('addCommitteeBtn').addEventListener('click', () => document.getElementById('committeeModalBackdrop').classList.add('open'));
@@ -1497,22 +1736,32 @@
     }
   });
   const verifiedThisMonth = allPayments.filter(p => p.status === 'verified' && inMonth(p.verifiedAt));
-  statusEl.textContent = `Building ${verifiedThisMonth.length} receipts + ${files.length} documents…`;
+
+  // The society record is the same for every receipt — it used to be fetched
+  // once per receipt inside the loop, which is one billed read per PDF.
+  const societySnap = await getDoc(doc(db, 'settings', 'society'));
+  const society = societySnap.exists() ? societySnap.data() : { fullName: SOCIETY.fullName };
+
+  let done = 0;
+  const total = verifiedThisMonth.length + files.length;
+  const tick = () => { statusEl.textContent = `Building ${++done} / ${total}…`; };
+
   for (const p of verifiedThisMonth) {
     try {
       const mem = allMembers.find(x => x.uid === p.memberUid) || {};
-      const societySnap = await getDoc(doc(db, 'settings', 'society'));
-      const society = societySnap.exists() ? societySnap.data() : { fullName: SOCIETY.fullName };
       const blob = await generateReceiptPDF({ payment: p, member: mem, society, logoDataUrl: null, save: false });
       files.push({ blob, name: `Receipts/${p.receiptNumber}.pdf` });
     } catch (e) { /* skip a failed receipt rather than aborting the whole export */ }
+    tick();
   }
 
   const zip = new window.JSZip();
   for (const f of files) {
-    if (f.blob) { zip.file(f.name, f.blob); continue; }
+    if (f.blob) { zip.file(f.name, f.blob); tick(); continue; }
     try { const b = await fetch(f.url).then(r => r.blob()); zip.file(f.name, b); } catch (e) {}
+    tick();
   }
+  statusEl.textContent = 'Compressing…';
   const zipBlob = await zip.generateAsync({ type: 'blob' });
   const a = document.createElement('a');
   a.href = URL.createObjectURL(zipBlob); a.download = `MHMRWS-${monthVal}.zip`; a.click();
@@ -1811,15 +2060,38 @@
 
     let memberID = null;
     if (f.alsoResident.checked) {
+      const tower = f.tower.value.trim();
+      const flatNumber = f.flatNumber.value.trim();
+      const claimRef = doc(db, 'flatClaims', flatClaimId(tower, flatNumber));
+      const held = await getDoc(claimRef);
+      if (held.exists()) throw new Error(`Tower ${tower}, Flat ${flatNumber} pehle se kisi aur member ke naam par hai.`);
+
       memberID = await generateMemberId();
-      await setDoc(doc(db, 'members', newUid), {
-        uid: newUid, name: f.name.value, fatherHusbandName: '', tower: f.tower.value.trim(),
-        flatNumber: f.flatNumber.value.trim(), mobile: f.mobile.value.trim(), email: f.email.value.trim(),
+      const publicToken = newPublicToken();
+
+      // This path used to write the member record only — no public record and
+      // no flat claim — so a committee member created here got a membership
+      // card whose QR resolved to "not found". Approving a resident normally
+      // and creating one from here now produce identical state.
+      const batch = writeBatch(db);
+      batch.set(doc(db, 'members', newUid), {
+        uid: newUid, name: f.name.value, fatherHusbandName: '', tower,
+        flatNumber, mobile: f.mobile.value.trim(), email: f.email.value.trim(),
         occupation: '', residentType: 'owner', address: '', nomineeName: '', nomineeRelation: '',
-        photoURL: '', idDocURL: '', declarationAccepted: true, memberID, status: 'approved', role: 'resident',
+        photoURL: '', idDocURL: '', declarationAccepted: true, memberID, publicToken,
+        status: 'approved', role: 'resident',
         source: 'admin_created', createdAt: serverTimestamp(), updatedAt: serverTimestamp(),
         approvedBy: currentAdmin.email, approvedAt: serverTimestamp()
       });
+      batch.set(doc(db, 'membersPublic', publicToken), {
+        memberID, name: f.name.value, tower, flatNumber,
+        residentType: 'owner', status: 'approved', updatedAt: serverTimestamp()
+      });
+      batch.set(claimRef, {
+        tower, flatNumber, memberDocId: newUid, memberUid: newUid, memberID, claimedAt: serverTimestamp()
+      });
+      await batch.commit();
+
       await reloadMembers(); renderMembersTable(); renderOverview(); populateTowerFilter();
     }
 
@@ -1832,16 +2104,16 @@
 });
 
 /* ---------------------------------------------------------------------- */
-/*  Audit Log                                                              */
+/*  Activity Log                                                           */
 /* ---------------------------------------------------------------------- */
 async function loadAuditLog() {
   try {
     const rows = (await getDocs(query(collection(db, 'auditLogs'), orderBy('timestamp', 'desc'), limit(200)))).docs.map(d => d.data());
     document.getElementById('auditLogBody').innerHTML = rows.map(r => `
       <tr><td>${fmtDateTime(r.timestamp)}</td><td>${escapeHtml(r.userEmail)}</td><td>${escapeHtml(r.action)}</td>
-      <td class="mono" style="font-size:11.5px;">${escapeHtml(JSON.stringify(r.details || {}))}</td><td>${escapeHtml(r.ip || '—')}</td></tr>
-    `).join('') || '<tr><td colspan="5" class="t-muted">No audit entries yet.</td></tr>';
-  } catch (e) { document.getElementById('auditLogBody').innerHTML = '<tr><td colspan="5" class="t-muted">Could not load audit log.</td></tr>'; }
+      <td class="mono" style="font-size:11.5px;">${escapeHtml(JSON.stringify(r.details || {}))}</td></tr>
+    `).join('') || '<tr><td colspan="4" class="t-muted">No activity recorded yet.</td></tr>';
+  } catch (e) { document.getElementById('auditLogBody').innerHTML = '<tr><td colspan="4" class="t-muted">Could not load activity log.</td></tr>'; }
 }
 </script>
 <style>.vrow{ display:flex; justify-content:space-between; padding:9px 0; border-bottom:1px solid var(--line); } .vrow:last-child{ border-bottom:none; } .vrow span:first-child{ color:var(--ink-400); }</style>
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/app-common.js MHMRWS-Portal/app-common.js
--- a/MHMRWS-Portal/app-common.js	2026-07-23 11:23:40.000000000 +0000
+++ MHMRWS-Portal/app-common.js	2026-07-25 12:51:53.898900973 +0000
@@ -14,22 +14,44 @@
 /* ---------------------------------------------------------------------- */
 /*  Toasts                                                                 */
 /* ---------------------------------------------------------------------- */
-export function showToast(message, type = 'info') {
+/* The live region has to already be in the document when a message is
+   inserted into it. Creating the container and the toast in the same tick —
+   which is what happened before — means assistive technology frequently never
+   announces the first one, and the first one is usually the important one
+   ("verify your email before paying"). So the region is created at load. */
+function ensureToastRegion() {
   let region = document.getElementById('toast-region');
-  if (!region) {
-    region = document.createElement('div');
-    region.id = 'toast-region';
-    document.body.appendChild(region);
+  if (region) return region;
+  region = document.createElement('div');
+  region.id = 'toast-region';
+  region.setAttribute('role', 'status');
+  region.setAttribute('aria-live', 'polite');
+  region.setAttribute('aria-atomic', 'false');
+  document.body.appendChild(region);
+  return region;
+}
+
+if (typeof document !== 'undefined') {
+  if (document.readyState === 'loading') {
+    document.addEventListener('DOMContentLoaded', ensureToastRegion, { once: true });
+  } else {
+    ensureToastRegion();
   }
+}
+
+export function showToast(message, type = 'info') {
+  const region = ensureToastRegion();
   const el = document.createElement('div');
   el.className = `toast ${type}`;
-  el.setAttribute('role', 'status');
+  // No role here: the region above already announces its own changes, and
+  // role="status" on both makes some screen readers read the message twice.
   el.textContent = message;
   region.appendChild(el);
   setTimeout(() => {
-    el.style.transition = 'opacity .3s ease';
-    el.style.opacity = '0';
-    setTimeout(() => el.remove(), 300);
+    // Class-based so the exit direction can differ between desktop (top-right,
+    // leaves upward) and mobile (bottom, leaves downward) — see styles.css.
+    el.classList.add('leaving');
+    setTimeout(() => el.remove(), 320);
   }, 4200);
 }
 
@@ -101,47 +123,47 @@
   const nomineeRelation = val('nomineeRelation');
   const residentType = val('residentType');
 
-  if (!name) return 'Naam zaroori hai.';
+  if (!name) return { field: 'name', message: 'Naam zaroori hai.' };
   if (name.length > LIMITS.nameMax) return `Naam ${LIMITS.nameMax} characters se lamba nahi ho sakta.`;
   // \p{M} matters here: Devanagari matras (ा ि ो) are Unicode *Marks*, not
   // Letters, so without it every Hindi name would be rejected.
-  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(name)) return 'Naam mein sirf akshar, space, aur . \' - ho sakte hain.';
+  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(name)) return { field: 'name', message: 'Naam mein sirf akshar, space, aur . \' - ho sakte hain.' };
 
-  if (!father) return 'Pita / Pati ka naam zaroori hai.';
-  if (father.length > LIMITS.nameMax) return 'Pita / Pati ka naam bahut lamba hai.';
-  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(father)) return 'Pita / Pati ke naam mein sirf akshar aur space ho sakte hain.';
-
-  if (!tower) return 'Tower chunna zaroori hai.';
-  if (!TOWER_PLAN[tower]) return 'Tower valid nahi hai.';
-  if (!flat) return 'Flat number chunna zaroori hai.';
+  if (!father) return { field: 'fatherHusbandName', message: 'Pita / Pati ka naam zaroori hai.' };
+  if (father.length > LIMITS.nameMax) return { field: 'fatherHusbandName', message: 'Pita / Pati ka naam bahut lamba hai.' };
+  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(father)) return { field: 'fatherHusbandName', message: 'Pita / Pati ke naam mein sirf akshar aur space ho sakte hain.' };
+
+  if (!tower) return { field: 'tower', message: 'Tower chunna zaroori hai.' };
+  if (!TOWER_PLAN[tower]) return { field: 'tower', message: 'Tower valid nahi hai.' };
+  if (!flat) return { field: 'flatNumber', message: 'Flat number chunna zaroori hai.' };
   if (!isValidFlat(tower, flat)) return `Flat ${flat} Tower ${tower} mein maujood nahi hai.`;
 
   // Indian mobile numbers begin 6, 7, 8 or 9 — this rejects landlines and
   // the common habit of typing a 0 or +91 prefix into the field.
-  if (!/^[0-9]{10}$/.test(mobile)) return 'Mobile number theek 10 ankon ka hona chahiye (bina 0 ya +91 ke).';
-  if (!/^[6-9]/.test(mobile)) return 'Mobile number 6, 7, 8 ya 9 se shuru hona chahiye.';
+  if (!/^[0-9]{10}$/.test(mobile)) return { field: 'mobile', message: 'Mobile number theek 10 ankon ka hona chahiye (bina 0 ya +91 ke).' };
+  if (!/^[6-9]/.test(mobile)) return { field: 'mobile', message: 'Mobile number 6, 7, 8 ya 9 se shuru hona chahiye.' };
 
-  if (!email) return 'Email zaroori hai.';
-  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) return 'Email address sahi format mein nahi hai.';
+  if (!email) return { field: 'email', message: 'Email zaroori hai.' };
+  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) return { field: 'email', message: 'Email address sahi format mein nahi hai.' };
 
-  if (!occupation) return 'Occupation zaroori hai.';
-  if (occupation.length > 100) return 'Occupation bahut lamba hai.';
-  if (!residentType) return 'Owner ya Tenant chunna zaroori hai.';
-  if (!['owner', 'tenant'].includes(residentType)) return 'Owner / Tenant valid nahi hai.';
+  if (!occupation) return { field: 'occupation', message: 'Occupation zaroori hai.' };
+  if (occupation.length > 100) return { field: 'occupation', message: 'Occupation bahut lamba hai.' };
+  if (!residentType) return { field: 'residentType', message: 'Owner ya Tenant chunna zaroori hai.' };
+  if (!['owner', 'tenant'].includes(residentType)) return { field: 'residentType', message: 'Owner / Tenant valid nahi hai.' };
 
-  if (!address) return 'Address zaroori hai.';
-  if (address.length > LIMITS.addressMax) return 'Address bahut lamba hai.';
+  if (!address) return { field: 'address', message: 'Address zaroori hai.' };
+  if (address.length > LIMITS.addressMax) return { field: 'address', message: 'Address bahut lamba hai.' };
 
-  if (!nomineeName) return 'Nominee ka naam zaroori hai.';
-  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(nomineeName)) return 'Nominee ke naam mein sirf akshar aur space ho sakte hain.';
-  if (!nomineeRelation) return 'Nominee se sambandh zaroori hai.';
+  if (!nomineeName) return { field: 'nomineeName', message: 'Nominee ka naam zaroori hai.' };
+  if (!/^[\p{L}\p{M}\s.'-]+$/u.test(nomineeName)) return { field: 'nomineeName', message: 'Nominee ke naam mein sirf akshar aur space ho sakte hain.' };
+  if (!nomineeRelation) return { field: 'nomineeRelation', message: 'Nominee se sambandh zaroori hai.' };
 
-  if (!f.photo?.files?.length) return 'Apni photo upload karna zaroori hai.';
+  if (!f.photo?.files?.length) return { field: 'photo', message: 'Apni photo upload karna zaroori hai.' };
   // Aadhaar / PAN is deliberately NOT required — see the note in index.html.
 
-  if (f.password.value.length < 6) return 'Password kam se kam 6 characters ka hona chahiye.';
-  if (f.password.value !== f.confirmPassword.value) return 'Password match nahi kar raha.';
-  if (!f.declaration.checked) return 'Aage badhne ke liye declaration par tick karein.';
+  if (f.password.value.length < 6) return { field: 'password', message: 'Password kam se kam 6 characters ka hona chahiye.' };
+  if (f.password.value !== f.confirmPassword.value) return { field: 'confirmPassword', message: 'Password match nahi kar raha.' };
+  if (!f.declaration.checked) return { field: 'declaration', message: 'Aage badhne ke liye declaration par tick karein.' };
   return null;
 }
 
@@ -330,10 +352,21 @@
   return d.toLocaleString('en-IN', { day: '2-digit', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' });
 }
 
+/* Milliseconds from either a Firestore Timestamp or anything Date can parse,
+   for sorting mixed rows without each caller re-checking for .toDate. */
+export function tsMillis(ts) {
+  if (!ts) return 0;
+  const d = ts.toDate ? ts.toDate() : new Date(ts);
+  const n = d.getTime();
+  return Number.isFinite(n) ? n : 0;
+}
+
+/* Called once per cell while rendering tables of several hundred rows, so it
+   must not allocate a DOM node each time — the old createElement version was
+   measurably the slowest thing in renderMembersTable(). */
+const HTML_ESCAPES = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
 export function escapeHtml(str) {
-  const div = document.createElement('div');
-  div.textContent = String(str ?? '');
-  return div.innerHTML;
+  return String(str ?? '').replace(/[&<>"']/g, (c) => HTML_ESCAPES[c]);
 }
 
 export function debounce(fn, delay = 300) {
@@ -389,34 +422,63 @@
 }
 
 /* ---------------------------------------------------------------------- */
-/*  Audit log — immutable ledger of admin actions (userId + IP + action)   */
+/*  Public verification tokens                                             */
+/*                                                                          */
+/*  The QR code on a receipt or a membership card used to carry the         */
+/*  sequential number itself — MHMRWS-2026-000004 — and the public          */
+/*  verification collections were keyed by it. Anyone could therefore ask   */
+/*  for 000001, 000002, 000003 ... and walk out with the society's entire   */
+/*  resident directory and every payment it had ever banked, without an     */
+/*  account and without tripping anything.                                  */
+/*                                                                          */
+/*  The QR now carries this instead: 128 random bits, which is not a space  */
+/*  anyone walks. The human-readable receipt/member number is unchanged and */
+/*  still printed on the document — it just is not the lookup key any more. */
 /* ---------------------------------------------------------------------- */
-let cachedIP = null;
-export async function getClientIP() {
-  if (cachedIP) return cachedIP;
-  try {
-    const res = await fetch('https://api.ipify.org?format=json');
-    const data = await res.json();
-    cachedIP = data.ip;
-    return cachedIP;
-  } catch (e) {
-    return 'unavailable';
-  }
+export function newPublicToken() {
+  const bytes = crypto.getRandomValues(new Uint8Array(16));
+  return Array.from(bytes, (b) => b.toString(16).padStart(2, '0')).join('');
+}
+
+/* Records issued before the token change are still keyed by their sequential
+   number, so receipts and cards already in residents' hands keep verifying.
+   Everything issued from now on uses the token. */
+export function publicKeyForReceipt(payment) {
+  return payment?.publicToken || payment?.receiptNumber || '';
+}
+export function publicKeyForMember(member) {
+  return member?.publicToken || member?.memberID || '';
 }
 
+/* ---------------------------------------------------------------------- */
+/*  Activity log — append-only record of committee actions                 */
+/*                                                                          */
+/*  NOT an audit log, and deliberately no longer named like one. These      */
+/*  entries are written by the browser of the admin performing the action,  */
+/*  which means an admin who does not want an entry written simply does not */
+/*  write one. Treat it as "what the committee did", useful for answering   */
+/*  questions at an AGM — not as evidence in a dispute.                     */
+/*                                                                          */
+/*  The IP field is gone. It came from an ipify call made by the same       */
+/*  browser being logged, so it recorded whatever that browser chose to     */
+/*  report — an attacker-controlled value dressed up as forensic detail,    */
+/*  which is worse than no value at all. It also sent every admin's IP to a */
+/*  third party on every logged action. If the society ever needs a real    */
+/*  audit trail, these writes must move into a Cloud Function, where        */
+/*  context.auth and the true request IP are observed server-side.          */
+/* ---------------------------------------------------------------------- */
 export async function logAudit(user, action, details = {}) {
   try {
-    const ip = await getClientIP();
     await addDoc(collection(db, 'auditLogs'), {
       userId: user?.uid || 'system',
       userEmail: user?.email || 'system',
       action,
       details,
-      ip,
+      source: 'client',   // honest about where this came from
       timestamp: serverTimestamp()
     });
   } catch (e) {
-    console.warn('Audit log write failed:', e);
+    console.warn('Activity log write failed:', e);
   }
 }
 
@@ -432,9 +494,14 @@
   });
 }
 
-export function verifyUrlFor(receiptNumber) {
+/* Accepts a payment object. A bare string is still accepted so that any
+   caller not yet updated keeps working against the legacy lookup. */
+export function verifyUrlFor(payment) {
+  const p = (typeof payment === 'string') ? { receiptNumber: payment } : (payment || {});
   const base = window.location.origin + window.location.pathname.replace(/[^/]+$/, '');
-  return `${base}verify.html?receipt=${encodeURIComponent(receiptNumber)}`;
+  return p.publicToken
+    ? `${base}verify.html?r=${encodeURIComponent(p.publicToken)}`
+    : `${base}verify.html?receipt=${encodeURIComponent(p.receiptNumber || '')}`;
 }
 
 /* ---------------------------------------------------------------------- */
@@ -519,7 +586,7 @@
 
   // QR code
   try {
-    const qrUrl = verifyUrlFor(payment.receiptNumber);
+    const qrUrl = verifyUrlFor(payment);
     const qrData = await generateQR(qrUrl, 260);
     pdf.addImage(qrData, 'PNG', W - marginX - 74, y - 4, 74, 74);
     pdf.setFont('helvetica', 'normal'); pdf.setFontSize(7); pdf.setTextColor(124, 135, 156);
@@ -532,7 +599,7 @@
   y += 22;
   pdf.setFont('helvetica', 'normal'); pdf.setFontSize(8.5); pdf.setTextColor(124, 135, 156);
   pdf.text('This is a system-generated receipt from the MHMRWS Digital Portal.', marginX, y);
-  pdf.text(`Verify anytime at: ${verifyUrlFor(payment.receiptNumber)}`, marginX, y + 13);
+  pdf.text(`Verify anytime at: ${verifyUrlFor(payment)}`, marginX, y + 13);
 
   if (save) {
     pdf.save(`Receipt-${payment.receiptNumber}.pdf`);
@@ -541,6 +608,315 @@
   return pdf.output('blob');
 }
 
+/* ---------------------------------------------------------------------- */
+/*  Payment statement PDF — a whole financial year on one page             */
+/*                                                                          */
+/*  A single receipt proves one payment; residents also need the full year */
+/*  in one document — for a loan file, a rent agreement, an income proof.   */
+/*  This lists every verified payment for the chosen FY with a running      */
+/*  total, in the same navy-and-gold identity as the receipt, and paginates */
+/*  automatically once a year has more rows than fit on a page.             */
+/* ---------------------------------------------------------------------- */
+export async function generateStatementPDF({ payments, member, society, financialYear, logoDataUrl, save = true }) {
+  const { jsPDF } = window.jspdf;
+  const pdf = new jsPDF({ unit: 'pt', format: 'a4' });
+  const W = pdf.internal.pageSize.getWidth();
+  const H = pdf.internal.pageSize.getHeight();
+  const marginX = 48;
+
+  // Only verified payments belong on a statement — pending ones are not yet
+  // money the society acknowledges receiving. Sorted oldest-first so the
+  // running total reads down the page the way a passbook does.
+  const rows = (payments || [])
+    .filter(p => p.status === 'verified' && p.financialYear === financialYear)
+    .sort((a, b) => tsMillis(a.verifiedAt || a.submittedAt) - tsMillis(b.verifiedAt || b.submittedAt));
+
+  const drawHeader = async () => {
+    pdf.setFillColor(10, 27, 51);
+    pdf.rect(0, 0, W, 100, 'F');
+    if (logoDataUrl) {
+      try {
+        const sealImg = await sealOnWhiteDisc(logoDataUrl);
+        if (sealImg) {
+          const cx = marginX + 26, cy = 50, r = 27;
+          pdf.setFillColor(255, 255, 255); pdf.circle(cx, cy, r, 'F');
+          pdf.addImage(sealImg, 'JPEG', cx - r * 0.94, cy - r * 0.94, r * 1.88, r * 1.88);
+          pdf.setDrawColor(228, 199, 101); pdf.setLineWidth(1); pdf.circle(cx, cy, r, 'S');
+        }
+      } catch (e) {/* statement is still valid without the seal */}
+    }
+    pdf.setTextColor(255, 255, 255);
+    pdf.setFont('times', 'bold'); pdf.setFontSize(18);
+    pdf.text(society.fullName || 'Resident Welfare Society', marginX + 64, 46);
+    pdf.setFont('helvetica', 'normal'); pdf.setFontSize(10);
+    pdf.text(`Reg. No: ${society.regNumber || '—'}`, marginX + 64, 62);
+    pdf.setFont('helvetica', 'bold'); pdf.setFontSize(11);
+    pdf.text('PAYMENT STATEMENT', W - marginX, 46, { align: 'right' });
+    pdf.setFont('helvetica', 'normal'); pdf.setFontSize(9);
+    pdf.text(`FY ${financialYear}`, W - marginX, 62, { align: 'right' });
+  };
+
+  await drawHeader();
+
+  // Resident block
+  let y = 128;
+  pdf.setTextColor(16, 25, 43);
+  pdf.setFont('helvetica', 'normal'); pdf.setFontSize(8.5); pdf.setTextColor(124, 135, 156);
+  pdf.text('RESIDENT', marginX, y);
+  pdf.text('FLAT / TOWER', W / 2 + 10, y);
+  pdf.setFont('helvetica', 'bold'); pdf.setFontSize(12); pdf.setTextColor(16, 25, 43);
+  pdf.text(member?.name || '—', marginX, y + 16);
+  pdf.text(`${member?.flatNumber || '—'} / ${member?.tower || '—'}`, W / 2 + 10, y + 16);
+  if (member?.memberID) {
+    pdf.setFont('helvetica', 'normal'); pdf.setFontSize(9); pdf.setTextColor(124, 135, 156);
+    pdf.text(`Member ID: ${member.memberID}`, marginX, y + 30);
+  }
+  y += 50;
+
+  // Table header
+  const cols = { date: marginX, receipt: marginX + 96, mode: marginX + 250, amount: W - marginX };
+  const drawTableHead = () => {
+    pdf.setFillColor(245, 246, 250);
+    pdf.rect(marginX, y, W - marginX * 2, 22, 'F');
+    pdf.setFont('helvetica', 'bold'); pdf.setFontSize(8.5); pdf.setTextColor(70, 80, 102);
+    pdf.text('DATE', cols.date + 6, y + 15);
+    pdf.text('RECEIPT NO.', cols.receipt, y + 15);
+    pdf.text('MODE', cols.mode, y + 15);
+    pdf.text('AMOUNT', cols.amount - 6, y + 15, { align: 'right' });
+    y += 22;
+  };
+  drawTableHead();
+
+  pdf.setFont('helvetica', 'normal'); pdf.setFontSize(10);
+  let runningTotal = 0;
+
+  for (const p of rows) {
+    // New page when we run low, repeating the header and column titles.
+    if (y > H - 120) {
+      pdf.addPage();
+      y = 60;
+      drawTableHead();
+      pdf.setFont('helvetica', 'normal'); pdf.setFontSize(10);
+    }
+    runningTotal += Number(p.amount) || 0;
+    pdf.setTextColor(16, 25, 43);
+    pdf.text(fmtDate(p.verifiedAt || p.submittedAt), cols.date + 6, y + 15);
+    pdf.setFontSize(9);
+    pdf.text(String(p.receiptNumber || '—'), cols.receipt, y + 15);
+    pdf.setFontSize(10);
+    pdf.text((p.mode || '—').toUpperCase(), cols.mode, y + 15);
+    pdf.text(formatINR(p.amount), cols.amount - 6, y + 15, { align: 'right' });
+    pdf.setDrawColor(226, 230, 239); pdf.setLineWidth(0.5);
+    pdf.line(marginX, y + 22, W - marginX, y + 22);
+    y += 24;
+  }
+
+  if (!rows.length) {
+    pdf.setTextColor(124, 135, 156); pdf.setFont('helvetica', 'italic');
+    pdf.text(`No verified payments recorded for FY ${financialYear}.`, marginX, y + 18);
+    y += 30;
+  }
+
+  // Total band
+  y += 8;
+  pdf.setFillColor(10, 27, 51);
+  pdf.roundedRect(marginX, y, W - marginX * 2, 46, 8, 8, 'F');
+  pdf.setFont('helvetica', 'normal'); pdf.setFontSize(9); pdf.setTextColor(228, 199, 101);
+  pdf.text(`TOTAL PAID · FY ${financialYear}`, marginX + 16, y + 20);
+  pdf.setFont('times', 'bold'); pdf.setFontSize(18); pdf.setTextColor(255, 255, 255);
+  pdf.text(formatINR(runningTotal), W - marginX - 16, y + 30, { align: 'right' });
+  pdf.setFont('helvetica', 'normal'); pdf.setFontSize(8); pdf.setTextColor(180, 190, 205);
+  pdf.text(`${rows.length} payment${rows.length === 1 ? '' : 's'}`, marginX + 16, y + 34);
+
+  // Footer
+  y += 70;
+  pdf.setDrawColor(226, 230, 239); pdf.setLineWidth(0.6);
+  pdf.line(marginX, y, W - marginX, y);
+  pdf.setFont('helvetica', 'normal'); pdf.setFontSize(8.5); pdf.setTextColor(124, 135, 156);
+  pdf.text('This is a system-generated statement from the MHMRWS Digital Portal.', marginX, y + 16);
+  pdf.text(`Generated on ${fmtDate(new Date())}. Each receipt can be verified individually via its QR code.`, marginX, y + 28);
+
+  if (save) {
+    pdf.save(`Statement-${member?.flatNumber || 'MHMRWS'}-FY${financialYear}.pdf`);
+    return null;
+  }
+  return pdf.output('blob');
+}
+
+/* ---------------------------------------------------------------------- */
+/*  Shared UI helpers — skeletons, empty states, inline field errors        */
+/* ---------------------------------------------------------------------- */
+
+/* A block of placeholder rows shaped like a two-line list item with a pill on
+   the right — matches the payment history, notices and documents lists. Drop
+   the returned HTML into a container while its data loads. */
+export function skeletonList(count = 3) {
+  const row = `
+    <div class="sk-row">
+      <div class="sk-stack">
+        <div class="sk-line sk sk-w-60"></div>
+        <div class="sk-line sk sk-w-40"></div>
+      </div>
+      <div class="sk-line sk sk-pill"></div>
+    </div>`;
+  return row.repeat(count);
+}
+
+/* Placeholder rows for a table body; spans the given column count so the layout
+   does not collapse while loading. */
+export function skeletonTableRows(cols = 4, rows = 4) {
+  const widths = ['sk-w-60', 'sk-w-40', 'sk-w-80', 'sk-w-25', 'sk-w-60'];
+  const tr = () => `<tr>${Array.from({ length: cols }, (_, i) =>
+    `<td><div class="sk-line sk ${widths[i % widths.length]}"></div></td>`).join('')}</tr>`;
+  return tr().repeat(rows);
+}
+
+/* A consistent empty state everywhere — the same small seal, a heading and a
+   line of guidance — instead of the assortment of bare "No records" strings
+   the panels grew independently. */
+export function emptyState(heading, detail = '', { compact = false } = {}) {
+  const seal = `<svg class="seal" width="40" height="40" viewBox="0 0 24 24" aria-hidden="true">
+    <circle cx="12" cy="12" r="10.5" fill="none" stroke="var(--gold-500)" stroke-width="1.2"/>
+    <circle cx="12" cy="12" r="7.5" fill="none" stroke="var(--line)" stroke-width="1"/>
+    <path d="M8.5 12.2l2.4 2.4 4.6-4.8" fill="none" stroke="var(--gold-700)" stroke-width="1.4"
+      stroke-linecap="round" stroke-linejoin="round"/></svg>`;
+  return `<div class="empty${compact ? ' empty-sm' : ''}">${seal}
+    <h4>${escapeHtml(heading)}</h4>${detail ? `<p>${escapeHtml(detail)}</p>` : ''}</div>`;
+}
+
+/* Mark a form field as invalid with a message directly beneath it. Expects the
+   input to live inside a .field wrapper (the registration form's structure).
+   Returns false so callers can `if (!showFieldError(...)) return;`-style guard. */
+export function showFieldError(input, message) {
+  const field = input?.closest('.field');
+  if (!field) return false;
+  field.classList.add('has-error');
+  let el = field.querySelector('.field-error');
+  if (!el) { el = document.createElement('div'); el.className = 'field-error'; field.appendChild(el); }
+  el.textContent = message;
+  return false;
+}
+
+export function clearFieldErrors(form) {
+  form?.querySelectorAll('.field.has-error').forEach(f => f.classList.remove('has-error'));
+}
+
+/* ---------------------------------------------------------------------- */
+/*  Idle auto-logout                                                        */
+/*                                                                          */
+/*  A committee member who signs in on a shared or public device and walks  */
+/*  away leaves the financial panel open to whoever sits down next. This    */
+/*  watches for inactivity, shows a warning with a countdown, and signs the  */
+/*  user out if they do not respond. Any real interaction resets the timer.  */
+/*                                                                          */
+/*  Call startIdleLogout({ onLogout, warnAfterMs, graceMs }). Returns a stop */
+/*  function to call on sign-out so the timers do not outlive the session.   */
+/* ---------------------------------------------------------------------- */
+export function startIdleLogout({ onLogout, warnAfterMs = 25 * 60 * 1000, graceMs = 60 * 1000 } = {}) {
+  let warnTimer = null, graceTimer = null, banner = null, countdown = null;
+
+  const clearBanner = () => {
+    countdown && clearInterval(countdown); countdown = null;
+    banner && banner.remove(); banner = null;
+    graceTimer && clearTimeout(graceTimer); graceTimer = null;
+  };
+
+  const doLogout = () => { clearBanner(); stop(); onLogout?.(); };
+
+  const showWarning = () => {
+    if (banner) return;
+    let left = Math.round(graceMs / 1000);
+    banner = document.createElement('div');
+    banner.className = 'session-banner';
+    banner.setAttribute('role', 'alertdialog');
+    banner.innerHTML = `<span>Suraksha ke liye aapko <b class="sec">${left}</b> second mein logout kiya jaayega.</span>
+      <button class="stay">Logged in rahein</button>
+      <button class="out">Abhi logout</button>`;
+    document.body.appendChild(banner);
+    banner.querySelector('.stay').onclick = () => { clearBanner(); reset(); };
+    banner.querySelector('.out').onclick = doLogout;
+    countdown = setInterval(() => {
+      left -= 1;
+      const s = banner?.querySelector('.sec'); if (s) s.textContent = left;
+      if (left <= 0) doLogout();
+    }, 1000);
+    graceTimer = setTimeout(doLogout, graceMs);
+  };
+
+  const reset = () => {
+    if (banner) return;                 // don't reset while the warning is up
+    warnTimer && clearTimeout(warnTimer);
+    warnTimer = setTimeout(showWarning, warnAfterMs);
+  };
+
+  // Throttle: activity fires constantly, but we only need to push the timer.
+  let last = 0;
+  const onActivity = () => {
+    const now = Date.now();
+    if (now - last < 1000) return;
+    last = now;
+    reset();
+  };
+  const events = ['click', 'keydown', 'touchstart', 'scroll', 'mousemove'];
+  events.forEach(e => document.addEventListener(e, onActivity, { passive: true }));
+
+  function stop() {
+    warnTimer && clearTimeout(warnTimer);
+    clearBanner();
+    events.forEach(e => document.removeEventListener(e, onActivity));
+  }
+
+  reset();
+  return stop;
+}
+
+/* ---------------------------------------------------------------------- */
+/*  Pagination                                                              */
+/*                                                                          */
+/*  Tables that load an unbounded set — the member directory, the full      */
+/*  payment ledger — are fine today at a few hundred rows but grow every    */
+/*  year, and rendering all of them at once is what makes the admin panel   */
+/*  crawl on a phone. This renders a compact pager (‹ 1 … 4 5 6 … 12 ›) into */
+/*  a container; the caller slices its own rows and re-renders on change.   */
+/* ---------------------------------------------------------------------- */
+export function renderPager(container, { page, pageSize, total, onPage }) {
+  if (!container) return;
+  const pages = Math.max(1, Math.ceil(total / pageSize));
+  if (pages <= 1) { container.innerHTML = ''; return; }
+
+  page = Math.min(Math.max(1, page), pages);
+  const from = (page - 1) * pageSize + 1;
+  const to = Math.min(page * pageSize, total);
+
+  // Window of page numbers around the current one, with ellipses.
+  const nums = [];
+  const push = (n) => nums.push(n);
+  const window = 1;
+  for (let n = 1; n <= pages; n++) {
+    if (n === 1 || n === pages || (n >= page - window && n <= page + window)) push(n);
+    else if (nums[nums.length - 1] !== '…') push('…');
+  }
+
+  container.innerHTML = `
+    <div class="pager-info">${from}–${to} of ${total}</div>
+    <div class="pager-btns">
+      <button class="pager-btn" data-pg="${page - 1}" ${page === 1 ? 'disabled' : ''} aria-label="Previous page">‹</button>
+      ${nums.map(n => n === '…'
+        ? `<span class="pager-gap">…</span>`
+        : `<button class="pager-btn ${n === page ? 'is-current' : ''}" data-pg="${n}" ${n === page ? 'aria-current="page"' : ''}>${n}</button>`
+      ).join('')}
+      <button class="pager-btn" data-pg="${page + 1}" ${page === pages ? 'disabled' : ''} aria-label="Next page">›</button>
+    </div>`;
+
+  container.querySelectorAll('.pager-btn[data-pg]').forEach(b => {
+    if (b.disabled) return;
+    b.addEventListener('click', () => {
+      const p = parseInt(b.dataset.pg, 10);
+      if (p >= 1 && p <= pages && p !== page) onPage(p);
+    });
+  });
+}
+
 export function serializeForExport(rows) {
   return (rows || []).map(row => {
     const clean = {};
@@ -558,7 +934,7 @@
 /* ---------------------------------------------------------------------- */
 export async function printReceipt({ payment, member, society, logoDataUrl }) {
   let qrImg = '';
-  try { qrImg = await generateQR(verifyUrlFor(payment.receiptNumber), 200); } catch (e) {/* print still works without the QR */}
+  try { qrImg = await generateQR(verifyUrlFor(payment), 200); } catch (e) {/* print still works without the QR */}
 
   const area = document.getElementById('printReceiptArea');
   if (!area) { showToast('Print area is missing from this page.', 'error'); return; }
@@ -593,7 +969,7 @@
       </div>
       <div class="pr-foot">
         This is a system-generated receipt from the MHMRWS Digital Portal.<br>
-        Verify anytime at: ${escapeHtml(verifyUrlFor(payment.receiptNumber))}
+        Verify anytime at: ${escapeHtml(verifyUrlFor(payment))}
       </div>
     </div>
   `;
@@ -617,9 +993,12 @@
 /* ---------------------------------------------------------------------- */
 const CARD_W = 85.6, CARD_H = 54;   // mm — ISO/IEC 7810 ID-1, the usual card size
 
-export function memberVerifyUrl(memberID) {
+export function memberVerifyUrl(member) {
+  const m = (typeof member === 'string') ? { memberID: member } : (member || {});
   const base = window.location.origin + window.location.pathname.replace(/[^/]+$/, '');
-  return `${base}verify.html?member=${encodeURIComponent(memberID)}`;
+  return m.publicToken
+    ? `${base}verify.html?m=${encodeURIComponent(m.publicToken)}`
+    : `${base}verify.html?member=${encodeURIComponent(m.memberID || '')}`;
 }
 
 function loadImage(src) {
@@ -761,7 +1140,7 @@
 
   // --- QR ----------------------------------------------------------------
   try {
-    const qr = await generateQR(memberVerifyUrl(member.memberID), 320);
+    const qr = await generateQR(memberVerifyUrl(member), 320);
     const qs = 15.5, qx = CARD_W - M - qs, qy = 15.5;
     pdf.setFillColor(255, 255, 255);
     pdf.roundedRect(qx - 0.8, qy - 0.8, qs + 1.6, qs + 1.6, 0.8, 0.8, 'F');
@@ -939,22 +1318,15 @@
   return `https://wa.me/${withCountry}?text=${encodeURIComponent(message)}`;
 }
 
-/* ---------------------------------------------------------------------- */
-/*  Bulk ZIP export (uses JSZip — window.JSZip)                           */
-/* ---------------------------------------------------------------------- */
-export async function zipFilesFromUrls(fileList, zipFilename) {
-  const zip = new window.JSZip();
-  for (const f of fileList) {
-    try {
-      const res = await fetch(f.url);
-      const blob = await res.blob();
-      zip.file(f.name, blob);
-    } catch (e) { console.warn('Skipped file in zip (fetch failed):', f.name, e); }
-  }
-  const blob = await zip.generateAsync({ type: 'blob' });
-  const a = document.createElement('a');
-  a.href = URL.createObjectURL(blob);
-  a.download = zipFilename;
-  a.click();
-  URL.revokeObjectURL(a.href);
+/* Share sheet with no fixed recipient — WhatsApp lets the user pick the chat or
+   group. This is what a notice "Share on WhatsApp" button needs: the committee
+   posts to the society group, a resident forwards to a neighbour. */
+export function waShareText(message) {
+  return `https://wa.me/?text=${encodeURIComponent(message)}`;
 }
+
+/* NOTE: zipFilesFromUrls() used to live here. It fetched every file into
+   memory before zipping, which would fall over on a phone for a bulk export of
+   a few hundred ID scans — and nothing called it, since admin.html has its own
+   month-scoped version with progress reporting. Removed rather than left as a
+   trap for the next person who goes looking for a bulk-export helper. */
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/firebase-config.js MHMRWS-Portal/firebase-config.js
--- a/MHMRWS-Portal/firebase-config.js	2026-07-23 04:00:32.000000000 +0000
+++ MHMRWS-Portal/firebase-config.js	2026-07-25 13:20:05.870966782 +0000
@@ -9,6 +9,11 @@
    See SETUP-GUIDE.md, Step 2, for the full walkthrough.
    ========================================================================== */
 
+/* The apiKey below is NOT a secret — Firebase web keys are public by design and
+   the security rules are what protect your data. It is still worth restricting:
+   Google Cloud Console -> APIs & Services -> Credentials -> this key ->
+   "Application restrictions" -> Websites -> add only your Pages domain. That
+   stops somebody else's app from being billed to your project. */
 const firebaseConfig = {
   apiKey: "AIzaSyAoPrgFpUlkjSfnF0560RXkusqAY65ysXY",
   authDomain: "mhm-portal.firebaseapp.com",
@@ -24,7 +29,7 @@
    Create it in: Google Cloud Console → APIs & Services → Credentials
    → Create Credentials → OAuth client ID → Web application
    See SETUP-GUIDE.md, Step 5. */
-const GOOGLE_OAUTH_CLIENT_ID = "REPLACE_WITH_YOUR_OAUTH_CLIENT_ID.apps.googleusercontent.com";
+const GOOGLE_OAUTH_CLIENT_ID = "801279298623-hulscgf3756i0arebt5ql48fv6mrsj8q.apps.googleusercontent.com";
 
 /* Society identity — used in page titles, receipts and the audit log. */
 const SOCIETY = {
@@ -38,11 +43,50 @@
    no build step needed). Every page imports { auth, db, storage } from here.
    -------------------------------------------------------------------------- */
 import { initializeApp } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js";
+import { initializeAppCheck, ReCaptchaV3Provider } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-app-check.js";
 import { getAuth, setPersistence, browserLocalPersistence } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
 import { getFirestore } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
 import { getStorage } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-storage.js";
 
 export const app = initializeApp(firebaseConfig);
+
+/* --------------------------------------------------------------------------
+   APP CHECK — please turn this on.
+
+   Security rules answer "is this user allowed to do that". They cannot answer
+   "is this request even coming from our app". Without App Check, anyone can
+   talk to this Firebase project directly from a script: create accounts by the
+   thousand, hammer the public collections, and run up the bill on a society
+   that budgets in four figures. The rules would hold — but you would be paying
+   for every rejected request.
+
+   To enable (about half an hour, and free):
+     1. Google Cloud Console -> reCAPTCHA -> create a key of type
+        "Website" / reCAPTCHA v3, with this site's domain in the allow-list.
+     2. Firebase Console -> App Check -> register the web app with that key.
+     3. Paste the SITE key (the public one, safe to commit) below.
+     4. Watch Firebase Console -> App Check -> Metrics for a few days. Once
+        nearly all traffic shows as verified, click Enforce on Firestore,
+        Storage and Authentication.
+
+   Leaving the placeholder in place is safe: App Check simply stays off and the
+   portal behaves exactly as before.
+   -------------------------------------------------------------------------- */
+export const RECAPTCHA_V3_SITE_KEY = "REPLACE_WITH_YOUR_RECAPTCHA_V3_SITE_KEY";
+
+export const APP_CHECK_ENABLED = !RECAPTCHA_V3_SITE_KEY.startsWith("REPLACE_WITH");
+if (APP_CHECK_ENABLED) {
+  try {
+    initializeAppCheck(app, {
+      provider: new ReCaptchaV3Provider(RECAPTCHA_V3_SITE_KEY),
+      isTokenAutoRefreshEnabled: true
+    });
+  } catch (e) {
+    console.warn('App Check could not start; continuing without it.', e);
+  }
+} else {
+  console.warn('App Check is not configured — see the note in firebase-config.js.');
+}
 export const auth = getAuth(app);
 export const db = getFirestore(app);
 export const storage = getStorage(app);
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/firestore.rules MHMRWS-Portal/firestore.rules
--- a/MHMRWS-Portal/firestore.rules	2026-07-23 11:26:50.000000000 +0000
+++ MHMRWS-Portal/firestore.rules	2026-07-24 11:51:52.582942958 +0000
@@ -14,6 +14,12 @@
 //     good user experience; these rules are what actually protect the ledger.
 //   * Nothing in members/payments is ever hard-deleted — the app soft-deletes
 //     by flipping a status field, preserving the legal audit trail.
+//   * Once a payment is VERIFIED it is frozen. Corrections are made with a
+//     reversing entry, never by editing history — that is what an auditor
+//     expects to see.
+//   * The public (unauthenticated) collections are keyed by an unguessable
+//     random token, NOT by the sequential receipt/member number, so nobody can
+//     walk 000001..999999 and scrape the resident directory or the ledger.
 // ============================================================================
 
 service cloud.firestore {
@@ -26,6 +32,9 @@
     function isOwner(uid) {
       return isSignedIn() && request.auth.uid == uid;
     }
+    // Firestore caches get()/exists() per document per request, so the pair of
+    // lookups below costs at most two document reads for a whole evaluation,
+    // however many helpers call them.
     function isAdmin() {
       return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
     }
@@ -43,15 +52,33 @@
     function canApproveMembers() {
       return isAdmin() && adminRole() in ['superadmin', 'president', 'vicepresident', 'secretary', 'jointsecretary'];
     }
+    // Proof the caller clicked the link in their own inbox. Enforced on the
+    // money path, not on registration, because the account has to exist before
+    // Firebase can send the verification mail.
+    function emailVerified() {
+      return isSignedIn() && request.auth.token.email_verified == true;
+    }
 
     /* ---------- validation helpers ---------- */
+    //
+    // ABSENCE vs NULL — read this before editing anything below.
+    //
+    // Reading a field that is not present on the document raises an evaluation
+    // error, and an erroring rule DENIES. So `data.foo == null` is not a safe
+    // "foo is empty" test: it works when the client writes an explicit null and
+    // breaks with an opaque permission error when the client simply omits the
+    // field. Every optional field is therefore checked through absentOrNull().
+    //
+    function absentOrNull(data, field) {
+      return !(field in data) || data[field] == null;
+    }
     function isNonEmptyString(v, maxLen) {
       return v is string && v.size() > 0 && v.size() <= maxLen;
     }
-    function isOptionalString(v, maxLen) {
-      return v == null || (v is string && v.size() <= maxLen);
+    function isOptionalString(data, field, maxLen) {
+      return absentOrNull(data, field) || (data[field] is string && data[field].size() <= maxLen);
     }
-    // A 10-digit Indian mobile number, optionally left blank on admin-created rows.
+    // A 10-digit Indian mobile number.
     function isValidMobile(v) {
       return v is string && v.matches('^[0-9]{10}$');
     }
@@ -72,8 +99,16 @@
     function isValidFY(v) {
       return v is string && v.matches('^[0-9]{4}-[0-9]{2}$');
     }
+    // 32 hex characters from crypto.getRandomValues() — see newPublicToken()
+    // in app-common.js. 128 bits of entropy is not walkable.
+    function isPublicToken(v) {
+      return v is string && v.matches('^[0-9a-f]{32}$');
+    }
+    // Absence-safe: an update that adds a field which was not there before is
+    // a change, and an update that drops one is too.
     function unchanged(field) {
-      return request.resource.data[field] == resource.data[field];
+      return (field in request.resource.data) == (field in resource.data)
+          && (!(field in resource.data) || request.resource.data[field] == resource.data[field]);
     }
 
     // ---- Residents ------------------------------------------------------
@@ -82,10 +117,10 @@
         return isNonEmptyString(request.resource.data.name, 100)
             && isNonEmptyString(request.resource.data.tower, 50)
             && isNonEmptyString(request.resource.data.flatNumber, 30)
-            && isOptionalString(request.resource.data.fatherHusbandName, 100)
-            && isOptionalString(request.resource.data.address, 500)
-            && isOptionalString(request.resource.data.occupation, 100)
-            && isOptionalString(request.resource.data.nomineeName, 100)
+            && isOptionalString(request.resource.data, 'fatherHusbandName', 100)
+            && isOptionalString(request.resource.data, 'address', 500)
+            && isOptionalString(request.resource.data, 'occupation', 100)
+            && isOptionalString(request.resource.data, 'nomineeName', 100)
             && isValidMemberStatus(request.resource.data.status)
             && request.resource.data.residentType in ['owner', 'tenant'];
       }
@@ -100,32 +135,57 @@
                       && validMemberShape()
                       && request.resource.data.uid == uid
                       && request.resource.data.status == 'pending'
-                      && request.resource.data.memberID == null
+                      && absentOrNull(request.resource.data, 'memberID')
+                      && absentOrNull(request.resource.data, 'publicToken')
                       && request.resource.data.role == 'resident'
                       && isValidMobile(request.resource.data.mobile)
                     );
 
       // Admin edits: only office-bearers may change approval status or assign
       // a Member ID. Any other admin may correct ordinary profile fields.
+      //
       // Residents may edit their own profile but never self-approve, grant
-      // themselves a Member ID, reactivate themselves, or change their role.
+      // themselves a Member ID or verification token, reactivate themselves,
+      // change their role — or move themselves to a different flat. That last
+      // one matters: maintenance rates are per-tower, so an unpinned flat
+      // number is a way to change your own dues and what your card says.
       allow update: if (
                       isAdmin() && validMemberShape() && (
                         canApproveMembers()
-                        || (unchanged('status') && unchanged('memberID') && unchanged('role'))
+                        || (unchanged('status') && unchanged('memberID') && unchanged('role') && unchanged('publicToken'))
                       )
                     ) || (
                       isOwner(uid)
                       && validMemberShape()
                       && unchanged('status')
                       && unchanged('memberID')
+                      && unchanged('publicToken')
                       && unchanged('role')
                       && unchanged('uid')
+                      && unchanged('tower')
+                      && unchanged('flatNumber')
                     );
 
       allow delete: if false; // soft-delete only (status: 'inactive')
     }
 
+    // ---- One flat, one member -------------------------------------------
+    // Firestore cannot express "unique across a collection" in a rule, so
+    // uniqueness is bought with a document ID: flatClaims/A_101 either exists
+    // or it does not. approveMember() creates it inside the same transaction
+    // that approves the resident, so two people claiming the same flat cannot
+    // both win — the loser's transaction fails.
+    match /flatClaims/{claimId} {
+      allow read: if isAdmin();
+      allow create, update: if canApproveMembers()
+                            && isNonEmptyString(request.resource.data.memberDocId, 200)
+                            && isNonEmptyString(request.resource.data.tower, 50)
+                            && isNonEmptyString(request.resource.data.flatNumber, 30);
+      // Released when a member is rejected or deactivated, so the flat can be
+      // claimed by whoever moves in next.
+      allow delete: if canApproveMembers();
+    }
+
     // ---- Admin directory (Super Admin manages roles) --------------------
     match /admins/{uid} {
       allow read: if isAdmin();
@@ -143,40 +203,66 @@
 
       // A resident submitting a payment cannot pre-assign themselves a receipt
       // number, cannot mark it verified, and cannot post an invalid amount.
+      // A verified email is required here rather than at registration: this is
+      // the point where a record enters the society's books, and it is the
+      // cheapest possible defence against someone filing payments in a
+      // neighbour's name.
       allow create: if isSignedIn()
+                    && emailVerified()
                     && request.resource.data.memberUid == request.auth.uid
                     && isValidAmount(request.resource.data.amount)
                     && isValidPaymentMode(request.resource.data.mode)
                     && isValidFY(request.resource.data.financialYear)
                     && request.resource.data.status in ['pending_approval', 'pending_verification']
-                    && request.resource.data.receiptNumber == null
+                    && absentOrNull(request.resource.data, 'receiptNumber')
+                    && absentOrNull(request.resource.data, 'publicToken')
                     && isNonEmptyString(request.resource.data.residentName, 100)
                     && isNonEmptyString(request.resource.data.flatNumber, 30)
-                    && isOptionalString(request.resource.data.utrOrChequeNo, 40);
+                    && isOptionalString(request.resource.data, 'utrOrChequeNo', 40);
 
       // Verifying a payment issues a receipt number and moves money in the
-      // books, so it is restricted to the Treasurer / Super Admin. The amount
-      // and payer can never be edited after submission.
+      // books, so it is restricted to the Treasurer / Super Admin.
+      //
+      // A VERIFIED PAYMENT IS FROZEN. Not just the amount and the payer — the
+      // whole record. Un-verifying a receipt that has already been printed,
+      // QR-coded and handed to a resident would leave the society unable to
+      // explain its own paperwork. Mistakes are corrected with a reversing
+      // entry, which is what the accounts want anyway.
       allow update: if canVerifyPayments()
+                    && resource.data.status != 'verified'
                     && isValidPaymentStatus(request.resource.data.status)
                     && unchanged('amount')
                     && unchanged('memberUid')
                     && unchanged('mode')
-                    && unchanged('financialYear');
+                    && unchanged('financialYear')
+                    && unchanged('submittedAt')
+                    // A receipt number, once issued, is never rewritten.
+                    && (absentOrNull(resource.data, 'receiptNumber') || unchanged('receiptNumber'))
+                    && (absentOrNull(resource.data, 'publicToken') || unchanged('publicToken'))
+                    // Marking something verified must actually produce a receipt.
+                    && (request.resource.data.status != 'verified' || (
+                          isNonEmptyString(request.resource.data.receiptNumber, 40)
+                          && isPublicToken(request.resource.data.publicToken)
+                       ));
 
       allow delete: if false;
     }
 
     // ---- Sequence counters (Member ID / Receipt No.) ---------------------
-    // These are written ONLY by admin flows (approve member, verify payment,
-    // bulk import). Leaving this open to any signed-in user would let a
-    // resident rewind the receipt sequence and force duplicate receipt
-    // numbers — so it is locked to admins.
+    // Admin-only, and STRICTLY INCREASING. The previous version allowed any
+    // admin to write any positive integer, which meant the receipt sequence
+    // could be wound backwards — accidentally or otherwise — and reissue a
+    // number that is already printed on somebody's receipt. Duplicate receipt
+    // numbers in a financial record are exactly what an auditor picks up on.
     match /counters/{counterId} {
       allow read: if isAdmin();
-      allow write: if isAdmin()
-                   && request.resource.data.value is int
-                   && request.resource.data.value > 0;
+      allow create: if isAdmin()
+                    && request.resource.data.value is int
+                    && request.resource.data.value > 0;
+      allow update: if isAdmin()
+                    && request.resource.data.value is int
+                    && request.resource.data.value > resource.data.value;
+      allow delete: if false;
     }
 
     // ---- Notice board — public read, office-bearer write ------------------
@@ -209,10 +295,18 @@
     }
 
     // ---- Public receipt verification (where the receipt QR lands) ---------
-    // Deliberately world-readable so anyone can check a receipt without an
-    // account. Holds only non-sensitive fields — never contact details,
-    // documents or IDs. Written as part of payment verification.
-    match /receiptsPublic/{receiptNumber} {
+    //
+    // KEYED BY RANDOM TOKEN, NOT BY RECEIPT NUMBER. This collection is
+    // world-readable so a resident can check a receipt without an account —
+    // but when the document ID was the sequential receipt number, anyone could
+    // request MHMRWS-2026-000001, 000002, 000003 ... and walk out with every
+    // payer name, flat and amount the society had ever banked, which totals to
+    // the society's income. The 128-bit token in the QR code fixes that: a
+    // scan still resolves instantly, a crawler gets nothing.
+    //
+    // Documents written before this change are still keyed by receipt number,
+    // so receipts already in residents' hands keep verifying. See MIGRATION.md.
+    match /receiptsPublic/{token} {
       allow read: if true;
       allow create: if canVerifyPayments()
                     && isValidAmount(request.resource.data.amount)
@@ -251,10 +345,15 @@
     }
 
     // ---- Public membership verification (where the card QR lands) ---------
-    // World-readable by design: a guard at the gate scans the card without an
-    // account. Holds only what that check needs — name, tower, flat, type and
-    // status. Never contact details, documents or payment history.
-    match /membersPublic/{memberID} {
+    //
+    // Same reasoning as receiptsPublic: keyed by random token so a guard at
+    // the gate can scan a card without an account, while nobody can enumerate
+    // MHM-2026-000001 upwards and walk off with the society's whole resident
+    // directory — every name, tower, flat and owner/tenant status.
+    //
+    // Holds only what the gate check needs. Never contact details, documents
+    // or payment history.
+    match /membersPublic/{token} {
       allow read: if true;
       allow create, update: if canApproveMembers()
                             && isNonEmptyString(request.resource.data.memberID, 40)
@@ -262,7 +361,14 @@
       allow delete: if false;
     }
 
-    // ---- Audit log — append-only, never editable --------------------------
+    // ---- Activity log — append-only, never editable ------------------------
+    //
+    // Deliberately NOT called an audit log any more. It is written by the
+    // browser, so a determined admin can simply not write an entry, and the
+    // only field these rules can vouch for is userId. It is a useful record of
+    // what the committee did; it is not evidence. If the society ever needs
+    // something that would stand up in a dispute, the writes have to move into
+    // a Cloud Function that reads context.auth and the real request IP.
     match /auditLogs/{logId} {
       allow read: if isAdmin();
       allow create: if isAdmin() && request.resource.data.userId == request.auth.uid;
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/index.html MHMRWS-Portal/index.html
--- a/MHMRWS-Portal/index.html	2026-07-23 11:25:48.000000000 +0000
+++ MHMRWS-Portal/index.html	2026-07-25 15:51:22.632325284 +0000
@@ -2,6 +2,41 @@
 <html lang="en">
 <head>
 <meta charset="UTF-8">
+<!-- ==========================================================================
+     CONTENT SECURITY POLICY
+     GitHub Pages cannot set HTTP headers, so this goes in a meta tag — which
+     must stay above every script and stylesheet it governs.
+
+     What this buys: an injected <script src="https://evil.example/x.js"> will
+     not load, and stolen data cannot be POSTed anywhere except the endpoints
+     listed in connect-src. That is the realistic attack against a page that
+     handles Aadhaar uploads and payment records.
+
+     What it does NOT buy: script-src still has 'unsafe-inline', because every
+     page here is built from inline module scripts and inline style attributes. Removing that needs the inline scripts moved into their
+     own .js files first — worth doing, but it is a refactor, not a patch.
+
+     frame-ancestors is not supported in a meta tag, so this cannot stop the
+     site being framed. If you ever move off GitHub Pages, set
+     `X-Frame-Options: DENY` (or frame-ancestors 'none') as a real header.
+
+     AFTER DEPLOYING: open the site, press F12, and check the Console for
+     "Refused to ..." messages. Any legitimate endpoint this policy blocks will
+     announce itself there and just needs adding to the right directive.
+     ========================================================================== -->
+<meta http-equiv="Content-Security-Policy" content="
+  default-src 'self';
+  script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://www.gstatic.com https://accounts.google.com https://apis.google.com https://www.google.com;
+  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
+  img-src 'self' data: blob: https://firebasestorage.googleapis.com https://*.googleusercontent.com;
+  font-src 'self' data: https://fonts.gstatic.com;
+  connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://accounts.google.com;
+  frame-src https://www.google.com https://accounts.google.com;
+  worker-src 'self';
+  object-src 'none';
+  base-uri 'self';
+  form-action 'self';
+">
 <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
 <title>Max Heights Majestic RWA — Digital Membership Portal</title>
 <meta name="description" content="Register, pay and download QR-verified receipts for Max Heights Majestic Resident Welfare Society.">
@@ -9,9 +44,25 @@
 <link rel="manifest" href="manifest.json">
 <link rel="icon" href="favicon-32.png">
 <link rel="apple-touch-icon" href="icon-192.png">
+<meta name="color-scheme" content="light">
+<link rel="preconnect" href="https://fonts.googleapis.com">
+<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link rel="stylesheet" href="styles.css">
-<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.2/jspdf.umd.min.js"></script>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcode/1.5.3/qrcode.min.js"></script>
+<!-- Third-party libraries.
+     These are loaded from a CDN with no integrity hash, which means a
+     compromised or hijacked CDN gets script execution on a page that handles
+     ID documents and payment records. Fix this — it takes two minutes:
+
+       1. Open https://cdnjs.com and search for the library.
+       2. Click the copy button next to "SRI" for the exact version below.
+       3. Paste the sha384-... value into an integrity="" attribute here.
+
+     Stronger still: download these five files into a vendor/ folder in this
+     repo and load them from './vendor/...'. Same-origin scripts need no hash,
+     cannot be tampered with by a third party, and let the service worker cache
+     them for genuine offline use. -->
+<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.2/jspdf.umd.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcode/1.5.3/qrcode.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
 <style>
 /* =====================================================================
    index.html — Resident Portal
@@ -148,60 +199,118 @@
 .stat-strip .stat span{ font-size:11.5px; color:rgba(255,255,255,.58); letter-spacing:.02em; }
 
 /* ---------- Birds ----------
-   Three small silhouettes drifting across the hero, echoing the birds in
-   the project's own brochure. Deliberately slow, faint and few — this is
-   meant to add a little life to a still image, not to draw the eye away
-   from the copy. Everything animates via transform only, so it stays
-   smooth on a phone and never triggers layout work. The hero already has
-   overflow:hidden, so birds leaving the frame cannot create a scrollbar. */
-/* z-index 2 puts the birds above the hero copy. They were previously at the
-   same level as .wrap, which comes later in the DOM and so painted straight
-   over them — two of the three were invisible. Passing in front is also the
-   truer effect: a bird crosses in front of a building, not behind it. They
-   are faint and only a few pixels tall, so they never impair reading, and
-   pointer-events:none keeps them from intercepting any tap. */
+   Small silhouettes drifting across the hero, echoing the birds in the
+   project's brochure. The point is a little life on a still image, not to pull
+   the eye off the copy: faint, few, slow.
+
+   Anatomy — this is what makes them read as birds rather than flapping Vs.
+   Each wing is two hinged segments, an "arm" (shoulder->wrist) and a "hand"
+   (wrist->tip). Real birds flex at the wrist, so on the downstroke the whole
+   wing spreads and on the upstroke the hand folds back and up. We get that by
+   nesting: the hand lives inside the arm, so it inherits the shoulder's
+   rotation and then adds its own — exactly how the joint behaves.
+
+   Cost — everything is transform: rotate()/translate() on the compositor.
+   Nothing morphs an SVG path per frame, nothing touches layout or paint. It
+   stays smooth on a cheap phone, which is the whole target here. */
 .birds{ position:absolute; inset:0; z-index:2; pointer-events:none; overflow:hidden; }
-.bird-path{ position:absolute; will-change:transform; animation:bird-fly linear infinite; }
-.bird-bob{ animation:bird-bob ease-in-out infinite; }
-.bird-bob svg{ display:block; color:#fff; animation:bird-wing ease-in-out infinite; transform-origin:50% 60%; }
+
+/* Horizontal drift across the hero, plus a gentle bob and bank so the flight
+   line curves instead of tracking dead straight through the headline. */
+.bird{ position:absolute; will-change:transform; animation:bird-fly linear infinite; }
+.bird-bob{ animation:bird-bob ease-in-out infinite; will-change:transform; }
+.bird-bank{ animation:bird-bank ease-in-out infinite; will-change:transform; }
+
+/* The bird itself: a body point with a wing either side. Sized off font-size
+   so one variable scales the whole creature. */
+.bird-body{ position:relative; width:0; height:0; font-size:15px; }
+/* Each wing is a zero-size anchor at the shoulder; the arm and hand are thin
+   bars drawn outward from it. Rotating this anchor swings the whole wing about
+   the shoulder. */
+/* The mirror sits on a static mount that the flap animation never touches, so
+   the wing keyframes can own `transform` outright. If scaleX lived on the same
+   element as the rotation, the animation would overwrite it and the left wing
+   would flap the wrong way. */
+.wing-mount{ position:absolute; left:0; top:0; width:0; height:0; }
+.wing-mount.left{ transform:scaleX(-1); }
+.wing{ position:absolute; left:0; top:0; width:0; height:0;
+  transform-origin:0 0;
+  animation:wing-arm ease-in-out infinite; }
+.wing .arm{ position:absolute; left:0; top:-0.05em; width:0.54em; height:0.1em;
+  border-radius:0.05em; background:currentColor; transform-origin:0 50%; }
+/* The hand is anchored at the wrist — the far tip of the arm — and adds its own
+   rotation, so the outer wing bends past the inner one instead of staying in
+   line with it. Slightly thinner and tapered to a round cap so it reads as a
+   wingtip, not a stick. */
+.wing .hand{ position:absolute; left:0.53em; top:-0.04em; width:0.5em; height:0.08em;
+  background:currentColor; transform-origin:0 50%;
+  /* taper to a point: full height at the wrist, nothing at the tip */
+  clip-path:polygon(0 0, 100% 42%, 100% 58%, 0 100%);
+  animation:wing-hand ease-in-out infinite; }
+.bird-body{ color:#fff; }
 
 @keyframes bird-fly{
-  from{ transform:translateX(-10vw); }
-  to  { transform:translateX(115vw); }
+  from{ transform:translateX(-12vw); }
+  to  { transform:translateX(116vw); }
 }
+/* Vertical drift over the crossing — a long slow rise and fall, unrelated to
+   the wingbeat, so the path reads as a real glide-and-flap rather than a
+   bouncing sprite. */
 @keyframes bird-bob{
-  0%,100%{ transform:translateY(0); }
-  50%    { transform:translateY(-16px); }
-}
-/* Squashing the silhouette vertically reads as a wingbeat without having to
-   redraw the path on every frame. Kept shallow on purpose: a harder squash
-   made the smaller birds collapse to a 2px line and effectively disappear
-   for half of every beat. */
-@keyframes bird-wing{
-  0%,100%{ transform:scaleY(1); }
-  50%    { transform:scaleY(.62); }
-}
-
-/* Each bird gets its own size, height, speed and rhythm so the group never
-   looks like one shape copied three times. Heights are chosen against the
-   hero's actual layout: content starts around 8% on desktop, so bird-1 rides
-   the clear band above it and the other two sit in the looser areas beside
-   the seal rather than tracking straight through the headline. */
-.bird-1{ top:5%;  animation-duration:34s; animation-delay:-4s; }
-.bird-1 .bird-bob{ animation-duration:3.4s; }
-.bird-1 svg{ width:18px; opacity:.55; animation-duration:.62s; }
-
-.bird-2{ top:24%; animation-duration:46s; animation-delay:-19s; }
-.bird-2 .bird-bob{ animation-duration:4.6s; }
-.bird-2 svg{ width:13px; opacity:.45; animation-duration:.78s; }
-
-.bird-3{ top:12%; animation-duration:40s; animation-delay:-31s; }
-.bird-3 .bird-bob{ animation-duration:4s; }
-.bird-3 svg{ width:15px; opacity:.5;  animation-duration:.7s; }
+  0%   { transform:translateY(6px); }
+  50%  { transform:translateY(-18px); }
+  100% { transform:translateY(6px); }
+}
+/* A slight roll into the bob, the way a bird banks as it changes height. */
+@keyframes bird-bank{
+  0%,100%{ transform:rotate(4deg); }
+  50%    { transform:rotate(-5deg); }
+}
+/* The shoulder: strong down-sweep, softer recovery — birds spend more of the
+   beat pushing down than lifting up, so the curve is not a clean sine. */
+@keyframes wing-arm{
+  0%   { transform:rotate(-42deg); }   /* wings raised, top of the upstroke */
+  45%  { transform:rotate(30deg); }    /* driven down through the horizon */
+  60%  { transform:rotate(24deg); }    /* brief hold at the bottom */
+  100% { transform:rotate(-42deg); }   /* swept back up */
+}
+/* The wrist: leads slightly and travels further, so the hand whips through the
+   bottom of the stroke and folds on the way up. The phase offset from the arm
+   is what stops the wing looking like one rigid stick. */
+@keyframes wing-hand{
+  0%   { transform:rotate(-46deg); }   /* hand folded up and back */
+  45%  { transform:rotate(28deg); }    /* whips down, wing fully spread */
+  60%  { transform:rotate(22deg); }
+  100% { transform:rotate(-46deg); }
+}
+
+/* Each bird gets its own size, altitude, speed and beat so the group never
+   looks like one shape copied. Altitudes are set against the hero layout:
+   content starts around 8% on desktop, so bird-1 rides the clear band above it
+   and the others sit in the looser areas beside the seal. */
+.bird-1{ top:5%;  animation-duration:36s; animation-delay:-4s; }
+.bird-1 .bird-bob{ animation-duration:7.2s; }
+.bird-1 .bird-bank{ animation-duration:7.2s; }
+.bird-1 .bird-body{ font-size:17px; color:rgba(255,255,255,.62); }
+.bird-1 .wing{ animation-duration:.9s; }
+.bird-1 .wing .hand{ animation-duration:.9s; }
+
+.bird-2{ top:22%; animation-duration:52s; animation-delay:-24s; }
+.bird-2 .bird-bob{ animation-duration:9s; }
+.bird-2 .bird-bank{ animation-duration:9s; }
+.bird-2 .bird-body{ font-size:12px; color:rgba(255,255,255,.5); }
+.bird-2 .wing{ animation-duration:1.15s; }
+.bird-2 .wing .hand{ animation-duration:1.15s; }
+
+.bird-3{ top:12%; animation-duration:44s; animation-delay:-33s; }
+.bird-3 .bird-bob{ animation-duration:8s; }
+.bird-3 .bird-bank{ animation-duration:8s; }
+.bird-3 .bird-body{ font-size:14px; color:rgba(255,255,255,.55); }
+.bird-3 .wing{ animation-duration:1.02s; }
+.bird-3 .wing .hand{ animation-duration:1.02s; }
 
 @media (max-width:900px){
-  /* Two is plenty on a small screen. On mobile the seal occupies roughly
-     8-28% of the hero, so both birds sit above it in the clear band. */
+  /* Two is plenty on a small screen; both ride the clear band above the seal. */
   .bird-3{ display:none; }
   .bird-1{ top:3%; }
   .bird-2{ top:6%; }
@@ -268,6 +377,15 @@
 .notice-date .m{ font-family:var(--font-mono); font-size:9.5px; letter-spacing:.08em; color:var(--ink-400); text-transform:uppercase; margin-top:3px; }
 .notice-body h4{ font-size:15px; margin-bottom:5px; }
 .notice-body p{ font-size:13.5px; color:var(--ink-600); }
+.notice-actions{ margin-top:10px; }
+.notice-share{
+  display:inline-flex; align-items:center; gap:6px; padding:6px 12px;
+  border-radius:var(--radius-pill); font-size:12px; font-weight:600;
+  color:#128C7E; background:rgba(18,140,126,.08); border:1px solid rgba(18,140,126,.22);
+  text-decoration:none; transition:background .15s ease;
+}
+.notice-share:hover{ background:rgba(18,140,126,.15); }
+.notice-share svg{ color:#128C7E; }
 .lang-toggle{ display:inline-flex; background:var(--paper); border-radius:var(--radius-pill); padding:3px; border:1px solid var(--line); }
 .lang-toggle button{ border:none; background:transparent; padding:6px 14px; border-radius:var(--radius-pill); font-size:12.5px; font-weight:600; color:var(--ink-400); cursor:pointer; }
 .lang-toggle button.active{ background:var(--navy-900); color:var(--white); }
@@ -298,6 +416,14 @@
   width:100%; height:100%; object-fit:cover; display:block;
   transition:transform .7s cubic-bezier(.2,.7,.3,1), opacity .45s ease;
   opacity:0;                       /* revealed once the file has decoded */
+  /* THIS IS LOad-BEARING, do not remove. .shot::before below is absolutely
+     positioned, and a positioned pseudo-element paints above a static sibling
+     no matter what order they appear in. Without this, the blurred placeholder
+     sat permanently on top of the real photograph — the images looked blurry
+     forever, and only sharpened on hover because the hover rule applies a
+     transform to the <img>, which promotes it into the positioned paint layer.
+     Giving the image its own stacking position fixes it in both states. */
+  position:relative; z-index:1;
 }
 .shot img.loaded{ opacity:1; }
 /* A ~900-byte blurred version of the same photo, inlined so it paints on the
@@ -305,10 +431,15 @@
    grey box, and because .shot already has a fixed aspect ratio nothing
    shifts when the full image arrives. */
 .shot::before{
-  content:''; position:absolute; inset:0;
+  content:''; position:absolute; inset:0; z-index:0;
   background-image:var(--lqip); background-size:cover; background-position:center;
   filter:blur(12px); transform:scale(1.06);
+  transition:opacity .5s ease;
 }
+/* Once the real photo is up, drop the placeholder entirely. It is no longer
+   doing anything, and a 12px blur held underneath a semi-transparent edge can
+   bleed a soft halo around the image corners. */
+.shot.is-loaded::before{ opacity:0; }
 .shot:hover img{ transform:scale(1.045); }
 .shot figcaption{
   position:absolute; left:0; right:0; bottom:0; padding:26px 20px 16px;
@@ -408,15 +539,51 @@
 .pay-history-item{ display:flex; justify-content:space-between; align-items:center; padding:15px 0; border-bottom:1px solid var(--line); gap:12px; flex-wrap:wrap; }
 .pay-history-item:last-child{ border-bottom:none; }
 .dues-box{ border:1px solid var(--line); border-radius:var(--radius-md); overflow:hidden; }
-.dues-head{ display:flex; align-items:center; justify-content:space-between; gap:12px; padding:14px 16px; background:var(--paper); flex-wrap:wrap; }
-.dues-head .amt{ font-family:var(--font-display); font-size:26px; font-weight:600; color:var(--navy-950); line-height:1.1; }
+.dues-head{ display:flex; align-items:center; gap:18px; padding:18px 16px; background:var(--paper); flex-wrap:wrap; }
+.dues-head .amt{ font-family:var(--font-display); font-size:28px; font-weight:600; color:var(--navy-950); line-height:1.1; font-variant-numeric:tabular-nums; margin:3px 0 8px; }
 .dues-head .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--ink-400); }
 .dues-split{ display:flex; }
 .dues-split > div{ flex:1; padding:11px 16px; border-right:1px solid var(--line); font-size:13px; }
+.dues-split b{ font-variant-numeric:tabular-nums; }
 .dues-split > div:last-child{ border-right:none; }
 .dues-split b{ display:block; font-size:14.5px; color:var(--navy-950); margin-top:2px; }
-.dues-bar{ height:5px; background:var(--line); }
-.dues-bar span{ display:block; height:100%; background:var(--gradient-saffron); }
+/* ---------- Dues ring ----------
+   This is the number a resident opens the app to find, and it used to be a
+   sentence plus a 5px bar. A ring answers "am I clear or not" before any
+   reading happens — and it is the seal motif that runs through the rest of
+   the site doing actual work rather than decoration.
+
+   The arc is drawn with stroke-dashoffset over the circle's circumference.
+   Both values are handed in as custom properties from renderDues(), so the
+   fill animates on insert with no JavaScript timing games. The global
+   reduced-motion rule collapses the duration to ~0 and `both` holds the end
+   state, so it simply appears filled — never stuck empty. */
+.dues-ring{ position:relative; width:104px; height:104px; flex-shrink:0; }
+.dues-ring svg{ width:100%; height:100%; transform:rotate(-90deg); }   /* start at 12 o'clock */
+.dues-ring circle{ fill:none; stroke-width:9; stroke-linecap:round; }
+.dues-ring .trk{ stroke:var(--line); }
+.dues-ring .val{
+  stroke:var(--saffron-500);
+  stroke-dasharray:var(--c);
+  stroke-dashoffset:var(--c);
+  animation:ring-fill .9s cubic-bezier(.2,.7,.3,1) both;
+}
+@keyframes ring-fill{ to{ stroke-dashoffset:var(--off); } }
+.dues-ring .val.is-paid,
+.dues-ring .val.is-overpaid{ stroke:var(--success); }
+.dues-ring .val.is-unpaid{ stroke:var(--danger); }
+.dues-ring .pct{
+  position:absolute; inset:0; display:grid; place-items:center; text-align:center;
+  font-family:var(--font-display); font-weight:600; font-size:22px; color:var(--navy-950);
+  line-height:1; letter-spacing:-.02em; font-variant-numeric:tabular-nums;
+}
+.dues-ring .pct small{ display:block; font-family:var(--font-mono); font-weight:500;
+  font-size:8.5px; letter-spacing:.1em; text-transform:uppercase; color:var(--ink-400); margin-top:3px; }
+.dues-figures{ min-width:0; }
+@media (max-width:420px){
+  .dues-ring{ width:86px; height:86px; }
+  .dues-ring .pct{ font-size:19px; }
+}
 .detail-line{ display:flex; justify-content:space-between; gap:12px; padding:9px 0; border-bottom:1px solid var(--line); font-size:13.4px; }
 .detail-line:last-child{ border-bottom:none; }
 .detail-line span:first-child{ color:var(--ink-400); }
@@ -459,6 +626,8 @@
 .qr-box img{ width:172px; height:172px; margin:0 auto 12px; border-radius:8px; background:var(--white); padding:8px; }
 
 /* ---------- Mobile bottom tab bar (the main mobile UX upgrade) ---------- */
+/* Tab bar is ~66px tall plus the safe area; lift toasts clear of it. */
+@media (max-width: 900px){ :root{ --toast-bottom-gap:80px; } }
 .tabbar-mobile{
   display:none; position:fixed; bottom:0; left:0; right:0; z-index:70;
   background:rgba(10,27,51,.97); backdrop-filter:blur(14px);
@@ -584,6 +753,7 @@
     <g id="i-phone"><rect x="6" y="2.5" width="12" height="19" rx="2.6" fill="none" stroke="currentColor" stroke-width="1.7"/><path d="M10.5 18.5h3" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></g>
     <g id="i-home"><path d="M3.5 10.5L12 3.5l8.5 7M6 9.5v11h12v-11" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></g>
     <g id="i-bell"><path d="M12 3.2a5.6 5.6 0 00-5.6 5.6c0 5-2 6.4-2 6.4h15.2s-2-1.4-2-6.4A5.6 5.6 0 0012 3.2zM10.3 19a2 2 0 003.4 0" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></g>
+    <g id="i-whatsapp"><path d="M12 2.6a9.3 9.3 0 00-8 14l-1.3 4.8 4.9-1.3A9.3 9.3 0 1012 2.6zm0 1.7a7.6 7.6 0 016.6 11.4l-.2.3.8 2.8-2.9-.8-.3.2A7.6 7.6 0 1112 4.3z" fill="currentColor"/><path d="M9.3 7.4c-.2-.5-.4-.5-.6-.5h-.5a1 1 0 00-.7.3c-.3.3-.9.9-.9 2.1s.9 2.4 1 2.6c.1.2 1.7 2.8 4.3 3.8 2.1.8 2.6.7 3 .6.6-.1 1.4-.6 1.6-1.1.2-.6.2-1 .1-1.1l-.6-.3c-.3-.2-1.4-.7-1.6-.8-.2-.1-.4-.1-.5.1l-.7.9c-.1.2-.3.2-.5.1a6.3 6.3 0 01-1.9-1.2 7 7 0 01-1.3-1.6c-.1-.2 0-.4.1-.5l.4-.4.3-.5v-.4l-.8-1.9z" fill="currentColor"/></g>
     <g id="i-rupee"><path d="M7.5 4.5h9M7.5 8.5h9M15.5 4.5c0 3-2 4-5 4h-3l7.5 11" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></g>
     <g id="i-user"><circle cx="12" cy="8.2" r="3.9" fill="none" stroke="currentColor" stroke-width="1.7"/><path d="M4.6 20.2c.9-3.7 3.9-5.8 7.4-5.8s6.5 2.1 7.4 5.8" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></g>
     <g id="i-pin"><path d="M12 21.5s7-6.3 7-11.2A7 7 0 005 10.3c0 4.9 7 11.2 7 11.2z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><circle cx="12" cy="10" r="2.6" fill="none" stroke="currentColor" stroke-width="1.7"/></g>
@@ -598,7 +768,6 @@
          under one pixel, which anti-aliasing then washed away almost entirely. -->
     <g id="i-file"><path d="M6 3.5h8l4 4v13H6z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><path d="M13.8 3.5V8h4.2M9 12.5h6M9 16h6" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></g>
     <g id="i-download"><path d="M12 3.5v11M8 11l4 4 4-4M4.5 19.5h15" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></g>
-    <g id="i-bird"><path d="M1 9C6.5 9 8.5 2.5 12.5 2.5S18.5 9 20 9c1.5 0 3.5-6.5 7.5-6.5S37 9 39 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" fill="none" vector-effect="non-scaling-stroke"/></g>
     <g id="i-shield"><path d="M12 2.8l7.5 3v6c0 4.6-3.1 8.4-7.5 9.4-4.4-1-7.5-4.8-7.5-9.4v-6z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><path d="M9 12l2.2 2.2L15.4 10" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></g>
     <g id="i-pool"><path d="M3 17.5c1.6 0 1.6 1.4 3.2 1.4s1.6-1.4 3.2-1.4 1.6 1.4 3.2 1.4 1.6-1.4 3.2-1.4 1.6 1.4 3.2 1.4M7 15.5V6a2.2 2.2 0 014.4 0M13.6 15.5V6A2.2 2.2 0 0118 6M7 9.5h4.4M7 12.6h4.4" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></g>
     <g id="i-gym"><path d="M3.5 9.5v5M6.5 7v10M17.5 7v10M20.5 9.5v5M6.5 12h11" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></g>
@@ -642,9 +811,9 @@
 <section class="hero engrave" id="top" style="padding-top:70px;">
   <div class="hero-photo" aria-hidden="true"></div>
   <div class="birds" aria-hidden="true">
-    <div class="bird-path bird-1"><div class="bird-bob"><svg viewBox="0 0 40 14" fill="none"><use href="#i-bird"/></svg></div></div>
-    <div class="bird-path bird-2"><div class="bird-bob"><svg viewBox="0 0 40 14" fill="none"><use href="#i-bird"/></svg></div></div>
-    <div class="bird-path bird-3"><div class="bird-bob"><svg viewBox="0 0 40 14" fill="none"><use href="#i-bird"/></svg></div></div>
+    <div class="bird bird-1"><div class="bird-bob"><div class="bird-bank"><div class="bird-body"><div class="wing-mount right"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div><div class="wing-mount left"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div></div></div></div></div>
+    <div class="bird bird-2"><div class="bird-bob"><div class="bird-bank"><div class="bird-body"><div class="wing-mount right"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div><div class="wing-mount left"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div></div></div></div></div>
+    <div class="bird bird-3"><div class="bird-bob"><div class="bird-bank"><div class="bird-body"><div class="wing-mount right"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div><div class="wing-mount left"><div class="wing"><span class="arm"></span><span class="hand"></span></div></div></div></div></div></div>
   </div>
   <div class="wrap hero-grid">
     <div>
@@ -879,6 +1048,13 @@
       </form>
 
       <form id="registerForm" style="display:none;">
+        <div class="step-indicator" id="stepIndicator" style="display:none;">
+          <div class="step-dot" data-step="0"><span class="num">1</span><span class="lbl">Details</span></div>
+          <div class="step-line" data-line="0"></div>
+          <div class="step-dot" data-step="1"><span class="num">2</span><span class="lbl">Uploads</span></div>
+          <div class="step-line" data-line="1"></div>
+          <div class="step-dot" data-step="2"><span class="num">3</span><span class="lbl">Account</span></div>
+        </div>
         <fieldset>
           <legend>Resident Details</legend>
           <div class="form-2col">
@@ -940,12 +1116,20 @@
           </div>
         </fieldset>
 
-        <label class="checkbox-row" style="margin-bottom:20px;">
-          <input type="checkbox" name="declaration" required>
-          <span>I declare that the information provided above is true to the best of my knowledge, and I authorize MHMRWS to verify these details.</span>
-        </label>
+        <div class="final-only">
+          <label class="checkbox-row" style="margin-bottom:20px;">
+            <input type="checkbox" name="declaration" required>
+            <span>I declare that the information provided above is true to the best of my knowledge, and I authorize MHMRWS to verify these details.</span>
+          </label>
 
-        <button class="btn btn-primary btn-block" type="submit">Submit Registration</button>
+          <button class="btn btn-primary btn-block" type="submit">Submit Registration</button>
+        </div>
+
+        <!-- Back / Next; shown only when JS enables stepped mode. -->
+        <div class="step-nav" id="stepNav" style="display:none;">
+          <button type="button" class="btn btn-ghost" id="stepBack">Back</button>
+          <button type="button" class="btn btn-primary" id="stepNext">Next</button>
+        </div>
       </form>
     </div>
   </div>
@@ -1042,10 +1226,11 @@
      failed import blanks out the entire page's branding.
      ===================================================================== -->
 <script type="module">
-import { LOGO_DATA_URI } from './logo-data.js';
+import { LOGO_DATA_URI, getLogoDataUri } from './logo-data.js';
 import { initLang, setLang, translateSubtree, HI } from './i18n.js';
 import { TOWER_IDS, flatsForTower } from './tower-plan.js';
-window.__MHM_LOGO = LOGO_DATA_URI;
+window.__MHM_LOGO = LOGO_DATA_URI;          // image URL, fine for <img src>
+window.__MHM_LOGO_DATAURI = getLogoDataUri;  // call to get a data: URI for PDFs
 window.__translate = translateSubtree;
 window.__HI = HI;
 
@@ -1109,7 +1294,10 @@
   // Fade the real photo in over its blurred placeholder once it has decoded.
   // Images already complete (from cache) are marked immediately, otherwise
   // a cached image would stay invisible because 'load' never fires.
-  const reveal = () => img.classList.add('loaded');
+  const reveal = () => {
+    img.classList.add('loaded');
+    img.closest('.shot')?.classList.add('is-loaded');   // retires the blurred placeholder
+  };
   if (img.complete && img.naturalWidth > 0) reveal();
   else img.addEventListener('load', reveal);
 
@@ -1214,7 +1402,7 @@
 import { auth, db, storage, SOCIETY } from './firebase-config.js';
 import {
   createUserWithEmailAndPassword, signInWithEmailAndPassword, onAuthStateChanged,
-  signOut, sendPasswordResetEmail
+  signOut, sendPasswordResetEmail, sendEmailVerification, deleteUser, reload as reloadUser
 } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
 import {
   doc, getDoc, setDoc, addDoc, collection, query, where, orderBy, getDocs, serverTimestamp, limit
@@ -1226,7 +1414,9 @@
   showToast, escapeHtml, fmtDate, formatINR, currentFinancialYear,
   generateReceiptPDF, printReceipt, validatePayment, validateRegistration,
   generateMembershipCard, duesFor, DUES_LABEL, DUES_BADGE,
-  expenseSummary, fundPosition
+  expenseSummary, fundPosition,
+  generateStatementPDF, skeletonList, emptyState, showFieldError, clearFieldErrors,
+  waShareText
 } from './app-common.js';
 
 /* Branding, the year stamp and the committee fallback are handled by the
@@ -1264,6 +1454,82 @@
 }
 tabLogin.addEventListener('click', () => showTab('login'));
 tabRegister.addEventListener('click', () => showTab('register'));
+
+/* ---- Stepped registration (progressive enhancement) ----------------------
+   The three fieldsets become three steps. If anything here throws, the form is
+   left in its plain all-visible state, so registration never depends on this. */
+(function initSteps() {
+  const form = registerForm;
+  const fieldsets = Array.from(form.querySelectorAll('fieldset'));
+  const indicator = document.getElementById('stepIndicator');
+  const nav = document.getElementById('stepNav');
+  const backBtn = document.getElementById('stepBack');
+  const nextBtn = document.getElementById('stepNext');
+  if (fieldsets.length !== 3 || !indicator || !nav) return;   // markup changed; bail safely
+
+  form.classList.add('steps-on');
+  indicator.style.display = 'flex';
+  nav.style.display = 'flex';
+
+  // Which fieldset each field name lives on — powers goToStepForField on a
+  // submit-time error and per-step "Next" validation.
+  const stepOfField = {};
+  fieldsets.forEach((fs, i) => {
+    fs.classList.toggle('step-current', i === 0);
+    fs.querySelectorAll('[name]').forEach(el => { stepOfField[el.name] = i; });
+  });
+  // declaration lives with the final step for the purpose of jumping to it.
+  stepOfField['declaration'] = 2;
+
+  let current = 0;
+  const render = () => {
+    fieldsets.forEach((fs, i) => fs.classList.toggle('step-current', i === current));
+    indicator.querySelectorAll('.step-dot').forEach((d, i) => {
+      d.classList.toggle('active', i === current);
+      d.classList.toggle('done', i < current);
+    });
+    indicator.querySelectorAll('.step-line').forEach((l, i) => l.classList.toggle('done', i < current));
+    backBtn.style.visibility = current === 0 ? 'hidden' : 'visible';
+    nextBtn.textContent = current === fieldsets.length - 1 ? 'Review' : 'Next';
+    form.classList.toggle('at-last', current === fieldsets.length - 1);
+    // On the last step the real submit button (inside .final-only) is the CTA,
+    // so hide the "Next" to avoid two primary buttons.
+    nextBtn.style.display = current === fieldsets.length - 1 ? 'none' : 'block';
+  };
+
+  // Let the browser's own required/pattern checks gate each step's fields, so
+  // we get native messages for free and never advance past an invalid step.
+  const stepIsValid = () => {
+    const controls = fieldsets[current].querySelectorAll('input, select, textarea');
+    for (const c of controls) {
+      if (!c.checkValidity()) { c.reportValidity(); return false; }
+    }
+    return true;
+  };
+
+  nextBtn.addEventListener('click', () => {
+    if (!stepIsValid()) return;
+    if (current < fieldsets.length - 1) { current++; render(); form.scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }
+  });
+  backBtn.addEventListener('click', () => {
+    if (current > 0) { current--; render(); }
+  });
+
+  // Called by the submit handler when validateRegistration reports a field on a
+  // step that isn't currently shown.
+  window.__goToStepForField = (fieldName) => {
+    const step = stepOfField[fieldName];
+    if (step != null && step !== current) { current = step; render(); }
+  };
+
+  render();
+})();
+
+// Fallback no-op if steps didn't initialise, so the submit handler can call it
+// unconditionally.
+if (!window.__goToStepForField) window.__goToStepForField = () => {};
+function goToStepForField(form, field) { window.__goToStepForField(field); }
+
 window.__goToPortal = (tab) => {
   if (currentUser) { document.getElementById('dashboard').scrollIntoView({ behavior: 'smooth' }); return; }
   showTab(tab);
@@ -1338,12 +1604,29 @@
   e.preventDefault();
   const f = e.target;
   const btn = f.querySelector('button[type="submit"]');
+  clearFieldErrors(f);
   const regError = validateRegistration(f);
-  if (regError) return showToast(regError, 'error');
+  if (regError) {
+    // Show the message directly under the field that caused it, scroll it into
+    // view, and focus it — so the resident sees exactly what to fix instead of
+    // hunting back from a toast in the corner.
+    const input = f.elements[regError.field];
+    if (input && input.closest('.field')) {
+      showFieldError(input, regError.message);
+      // If the bad field is on a step that isn't showing, jump to that step.
+      goToStepForField(f, regError.field);
+      input.closest('.field').scrollIntoView({ behavior: 'smooth', block: 'center' });
+      try { input.focus({ preventScroll: true }); } catch (_) {}
+    } else {
+      showToast(regError.message, 'error');
+    }
+    return;
+  }
   setLoading(btn, true);
+  let cred = null;
   try {
     const email = f.email.value.trim();
-    const cred = await createUserWithEmailAndPassword(auth, email, f.password.value);
+    cred = await createUserWithEmailAndPassword(auth, email, f.password.value);
     const uid = cred.user.uid;
 
     let photoURL = '', idDocURL = '';
@@ -1372,15 +1655,38 @@
       updatedAt: serverTimestamp()
     });
 
+    // Proof the person owns the address they registered with. Without this,
+    // anyone could sign up in a neighbour's name using their email — and the
+    // committee would have no way to tell the difference at approval time.
+    // A mail that fails to send must not undo a valid registration — the
+    // resident can resend from the dashboard — but we must NOT claim the mail
+    // was sent when it was not, so the send result decides the message.
+    let verifySent = false, verifyErr = null;
+    try { await sendEmailVerification(cred.user); verifySent = true; }
+    catch (e) { verifyErr = e; console.warn('Verification mail failed:', e?.code, e?.message); }
+
     if (f.photo.files[0] && !photoURL) {
-      // The registration itself succeeded; only the file could not be stored.
       showToast('Registration jama ho gaya, lekin photo upload nahi ho payi. Office se photo add karwa lein.', 'error');
+    } else if (verifySent) {
+      showToast('Registration jama ho gaya! Apne email par verification link check karein (spam bhi dekh lein).', 'success');
     } else {
-      showToast('Registration jama ho gaya! Admin approval ke baad Member ID milegi.', 'success');
+      // The account exists; only the mail failed. Tell them plainly, and log
+      // the Firebase error code so the cause is diagnosable from the console.
+      showToast('Registration ho gaya, par verification email abhi nahi bhej paaye' +
+                (verifyErr?.code ? ' (' + verifyErr.code + ')' : '') +
+                '. Login karke dashboard se "Link dobara bhejein" try karein.', 'error');
     }
     f.reset();
     showTab('login');
   } catch (err) {
+    // If the member record never got written, the Auth account left behind is
+    // worse than no account at all: the resident cannot register again
+    // ("email already in use") and cannot log in to anything either. Roll it
+    // back so they can simply try once more.
+    if (cred?.user) {
+      try { await deleteUser(cred.user); }
+      catch (e) { console.warn('Could not roll back the half-created account:', e); }
+    }
     showToast(friendlyAuthError(err), 'error');
   } finally {
     setLoading(btn, false);
@@ -1449,12 +1755,31 @@
     inactive: '<span class="badge badge-neutral"><span class="badge-dot"></span>Deactivated</span>'
   };
 
+  /* The security rules require a verified email before a payment can be
+     filed. Without this banner that arrives as an unexplained permission
+     error at the worst possible moment — after the resident has already
+     transferred the money. Say it up front instead, and give them the button. */
+  const emailUnverified = !!currentUser && !currentUser.emailVerified;
+  const verifyBanner = emailUnverified ? `
+    <div class="card card-pad" style="border-left:4px solid var(--warning); margin-bottom:16px;">
+      <div style="font-weight:600; margin-bottom:5px;">Email verify karna baaki hai</div>
+      <p class="t-sub" style="font-size:13.5px; margin-bottom:12px;">
+        Payment submit karne se pehle apna email verify karein. Humne
+        <b>${escapeHtml(currentMember.email || '')}</b> par link bheja tha — inbox aur spam dono dekh lein.
+      </p>
+      <div style="display:flex; gap:8px; flex-wrap:wrap;">
+        <button class="btn btn-outline btn-sm" id="resendVerifyBtn">Link dobara bhejein</button>
+        <button class="btn btn-ghost btn-sm" id="recheckVerifyBtn">Ho gaya — dobara check karein</button>
+      </div>
+    </div>` : '';
+
   let statusExtra = '';
   if (currentMember.status === 'pending') statusExtra = '<p class="t-sub" style="margin-top:12px; font-size:13.5px;">Aapka registration committee ke review mein hai. Approve hone par yahan Member ID dikhega.</p>';
   if (currentMember.status === 'rejected') statusExtra = `<p class="t-danger" style="margin-top:12px; font-size:13.5px;">Reason: ${escapeHtml(currentMember.rejectionReason || 'Office se contact karein.')}</p>`;
   if (currentMember.status === 'inactive') statusExtra = '<p class="t-sub" style="margin-top:12px; font-size:13.5px;">Aapki membership deactivate ki gayi hai. Query ke liye office se contact karein.</p>';
 
   content.innerHTML = `
+    ${verifyBanner}
     <div class="card status-card">
       <div class="status-head">
         <img src="${currentMember.photoURL || LOGO_DATA_URI}" alt="">
@@ -1474,12 +1799,15 @@
     ${currentMember.status === 'approved' ? `
     <div class="dash-grid">
       <div class="card card-pad">
-        <div id="duesPanel"><div class="skeleton" style="height:74px; margin-bottom:18px;"></div></div>
+        <div id="duesPanel"><div class="skeleton" style="height:186px; margin-bottom:18px;"></div></div>
         <div class="spread" style="margin:18px 0 16px; gap:10px; flex-wrap:wrap;">
           <h3 style="font-size:16px;">Payment History</h3>
-          <button class="btn btn-primary btn-sm" id="openPaymentBtn">Make a Payment</button>
+          <div style="display:flex; gap:8px; flex-wrap:wrap;">
+            <button class="btn btn-outline btn-sm" id="downloadStatementBtn" title="Poore financial year ki receipts ek PDF mein">FY Statement</button>
+            <button class="btn btn-primary btn-sm" id="openPaymentBtn"${emailUnverified ? ' disabled title="Pehle apna email verify karein"' : ''}>Make a Payment</button>
+          </div>
         </div>
-        <div id="paymentList"><div class="skeleton" style="height:70px;"></div></div>
+        <div id="paymentList"></div>
       </div>
       <div class="card card-pad">
         <h3 style="font-size:16px; margin-bottom:12px;">Your Details</h3>
@@ -1494,13 +1822,70 @@
 
   window.__translate(content);
 
+  document.getElementById('resendVerifyBtn')?.addEventListener('click', async (e) => {
+    const b = e.currentTarget;
+    b.disabled = true;
+    try {
+      await sendEmailVerification(currentUser);
+      showToast('Verification link bhej diya. Inbox aur spam folder dono dekh lein.', 'success');
+      // Re-enable after a cooldown so a resident can retry, but not spam it.
+      setTimeout(() => { b.disabled = false; }, 30000);
+    } catch (err) {
+      console.warn('Resend failed:', err?.code, err?.message);
+      const msg = err?.code === 'auth/too-many-requests'
+        ? 'Bahut baar bhej diya — thodi der (5-10 min) baad try karein.'
+        : 'Link nahi bhej paaye' + (err?.code ? ' (' + err.code + ')' : '') + '. Thodi der baad try karein.';
+      showToast(msg, 'error');
+      b.disabled = false;
+    }
+  });
+
+  document.getElementById('recheckVerifyBtn')?.addEventListener('click', async () => {
+    // emailVerified is baked into the cached token, so it does not change on
+    // its own — the user object has to be reloaded from the server.
+    await reloadUser(currentUser);
+    if (currentUser.emailVerified) {
+      showToast('Email verify ho gaya. Ab aap payment kar sakte hain.', 'success');
+      await loadDashboard(uid);
+    } else {
+      showToast('Abhi tak verify nahi hua. Email mein diye link par click karein.', 'error');
+    }
+  });
+
   if (currentMember.status === 'approved') {
     document.getElementById('openPaymentBtn').addEventListener('click', openPaymentModal);
     document.getElementById('downloadCardBtn')?.addEventListener('click', downloadCard);
+    document.getElementById('downloadStatementBtn')?.addEventListener('click', downloadStatement);
     await loadPayments(uid);
   }
 }
 
+/* Full financial-year statement — every verified receipt on one PDF, for a
+   loan file or income proof. Uses the current FY; a resident who needs an
+   older year can be given it from the admin side. */
+async function downloadStatement(e) {
+  const btn = e.currentTarget;
+  const fy = currentFinancialYear();
+  const verified = allPaymentRows.filter(p => p.status === 'verified' && p.financialYear === fy);
+  if (!verified.length) {
+    showToast(`FY ${fy} mein koi verified payment nahi hai — statement banane ke liye kam se kam ek chahiye.`, 'error');
+    return;
+  }
+  setLoading(btn, true);
+  try {
+    const societySnap = await getDoc(doc(db, 'settings', 'society'));
+    const society = societySnap.exists() ? societySnap.data() : { fullName: SOCIETY.fullName };
+    let logoDataUrl = null;
+    try { logoDataUrl = await window.__MHM_LOGO_DATAURI(); } catch (_) {}
+    await generateStatementPDF({ payments: allPaymentRows, member: currentMember, society, financialYear: fy, logoDataUrl });
+  } catch (err) {
+    console.error(err);
+    showToast('Statement nahi ban paaya. Dobara try karein.', 'error');
+  } finally {
+    setLoading(btn, false);
+  }
+}
+
 /* ---- Membership card ---- */
 async function downloadCard() {
   const btn = document.getElementById('downloadCardBtn');
@@ -1534,6 +1919,7 @@
 
 /* ---- Payments ---- */
 let currentDues = null;   // recomputed whenever payments are loaded
+let allPaymentRows = [];  // full payment history for the signed-in member
 
 async function loadMaintenanceSettings() {
   try {
@@ -1560,16 +1946,32 @@
   const headline = dues.outstanding > 0 ? dues.outstanding : dues.paid;
   const headlineLabel = dues.outstanding > 0 ? 'Outstanding' : 'Paid in full';
 
+  /* Ring geometry. r=42 in a 100x100 viewBox leaves room for a 9px stroke
+     without clipping at the edges. The arc is the circumference minus the
+     share already paid, fed to stroke-dashoffset. */
+  const R = 42;
+  const CIRC = 2 * Math.PI * R;                       // 263.894...
+  const offset = CIRC * (1 - Math.min(pct, 100) / 100);
+
   el.innerHTML = `
     <div class="dues-box">
       <div class="dues-head">
-        <div>
+        <div class="dues-ring" role="progressbar"
+             aria-valuenow="${pct}" aria-valuemin="0" aria-valuemax="100"
+             aria-label="Maintenance paid for FY ${escapeHtml(financialYear)}">
+          <svg viewBox="0 0 100 100" aria-hidden="true">
+            <circle class="trk" cx="50" cy="50" r="${R}"></circle>
+            ${pct > 0 ? `<circle class="val is-${escapeHtml(dues.status)}" cx="50" cy="50" r="${R}"
+                    style="--c:${CIRC.toFixed(2)}; --off:${offset.toFixed(2)};"></circle>` : ''}
+          </svg>
+          <div class="pct" aria-hidden="true">${pct}%<small>Paid</small></div>
+        </div>
+        <div class="dues-figures">
           <div class="lbl">${escapeHtml(headlineLabel)} &middot; FY ${escapeHtml(financialYear)}</div>
           <div class="amt">${formatINR(headline)}</div>
+          <span class="badge ${DUES_BADGE[dues.status]}">${escapeHtml(DUES_LABEL[dues.status])}</span>
         </div>
-        <span class="badge ${DUES_BADGE[dues.status]}">${escapeHtml(DUES_LABEL[dues.status])}</span>
       </div>
-      <div class="dues-bar"><span style="width:${pct}%"></span></div>
       <div class="dues-split">
         <div><span class="lbl">Total Due</span><b>${formatINR(dues.expected)}</b></div>
         <div><span class="lbl">Paid</span><b>${formatINR(dues.paid)}</b></div>
@@ -1581,10 +1983,15 @@
 
 async function loadPayments(uid) {
   const list = document.getElementById('paymentList');
-  const q = query(collection(db, 'payments'), where('memberUid', '==', uid), orderBy('submittedAt', 'desc'), limit(10));
+  list.innerHTML = skeletonList(3);          // shaped placeholder, no "Loading…" jump
+  // Pull the whole history for this member: the dues panel and the FY statement
+  // both need every row, and one resident's payments are few. The list itself
+  // still shows only the most recent (below).
+  const q = query(collection(db, 'payments'), where('memberUid', '==', uid), orderBy('submittedAt', 'desc'));
   let rows;
   try { rows = (await getDocs(q)).docs.map(d => ({ id: d.id, ...d.data() })); }
   catch (e) { rows = []; }
+  allPaymentRows = rows;   // kept for the statement button
 
   // Dues are computed from the same rows the history renders, so the panel
   // and the list can never disagree with each other.
@@ -1592,7 +1999,14 @@
   currentDues = duesFor(currentMember, rows, currentFinancialYear(), maintenance);
   renderDues(currentDues, currentFinancialYear());
 
-  if (!rows.length) { list.innerHTML = '<div class="empty" style="padding:28px 12px;"><p>No payment records yet.</p></div>'; window.__translate(list); return; }
+  if (!rows.length) {
+    list.innerHTML = emptyState('Abhi tak koi payment nahi', 'Aapke verified payments aur receipts yahan dikhenge.', { compact: true });
+    window.__translate(list); return;
+  }
+
+  // Show only the 10 most recent in the list; the full set stays in
+  // allPaymentRows for the dues panel and the statement download.
+  const listRows = rows.slice(0, 10);
 
   const statusBadge = {
     pending_approval: '<span class="badge badge-warning">Pending Approval</span>',
@@ -1601,7 +2015,7 @@
     rejected: '<span class="badge badge-danger">Rejected</span>'
   };
 
-  list.innerHTML = rows.map(p => `
+  list.innerHTML = listRows.map(p => `
     <div class="pay-history-item">
       <div>
         <div style="font-weight:600; font-size:14.5px;">${formatINR(p.amount)} <span class="t-muted" style="font-weight:400;">· ${escapeHtml((p.mode||'').toUpperCase())}</span></div>
@@ -1618,12 +2032,23 @@
     </div>
   `).join('');
 
+  if (rows.length > listRows.length) {
+    const note = document.createElement('div');
+    note.className = 't-muted';
+    note.style.cssText = 'font-size:12px; text-align:center; padding:12px 0 2px;';
+    note.textContent = `${listRows.length} recent dikha rahe hain · poori history ke liye "FY Statement" download karein`;
+    list.appendChild(note);
+  }
+
   window.__translate(list);
 
   async function loadReceiptContext() {
     const societySnap = await getDoc(doc(db, 'settings', 'society'));
     const society = societySnap.exists() ? societySnap.data() : { fullName: SOCIETY.fullName };
-    return { society, logoDataUrl: LOGO_DATA_URI };
+    // jsPDF needs actual bytes, not a URL — fetch-and-cache the data URI.
+    let logoDataUrl = null;
+    try { logoDataUrl = await window.__MHM_LOGO_DATAURI(); } catch (e) { /* PDF still renders sans seal */ }
+    return { society, logoDataUrl };
   }
 
   list.querySelectorAll('[data-receipt-id]').forEach(btn => {
@@ -1652,6 +2077,12 @@
 }
 
 async function openPaymentModal() {
+  // Belt and braces: the button is disabled when the email is unverified, but
+  // this is the function the rules will reject, so it should say why itself.
+  if (currentUser && !currentUser.emailVerified) {
+    showToast('Payment se pehle apna email verify karein — dashboard par "Link dobara bhejein" dabayein.', 'error');
+    return;
+  }
   const q = query(collection(db, 'payments'), where('memberUid', '==', currentUser.uid), where('status', 'in', ['pending_approval', 'pending_verification']));
   const existing = await getDocs(q);
   if (!existing.empty) { showToast('Aapka ek payment pehle se pending hai — nayi entry ki zaroorat nahi.', 'error'); return; }
@@ -1659,8 +2090,31 @@
   const paySettingsSnap = await getDoc(doc(db, 'settings', 'payment'));
   const paySettings = paySettingsSnap.exists() ? paySettingsSnap.data() : {};
 
+  // The maintenance invoice — expected / paid / outstanding for this FY,
+  // straight from the committee's per-tower rate. This is what makes "type an
+  // amount" unnecessary: the bill is shown and the amount is pre-set to it.
+  const fy = currentFinancialYear();
+  const dues = currentDues || { expected: 0, paid: 0, outstanding: 0, status: 'no_rate' };
+  const hasRate = dues.expected > 0;
+  const invoiceHtml = hasRate ? `
+    <div class="invoice-card">
+      <div class="invoice-head"><span class="lbl">Maintenance Invoice</span><span class="fy">FY ${escapeHtml(fy)}</span></div>
+      <div class="invoice-rows">
+        <div class="r"><span class="muted">Flat ${escapeHtml(currentMember.flatNumber)} · Tower ${escapeHtml(currentMember.tower)} ka charge</span><span>${formatINR(dues.expected)}</span></div>
+        <div class="r"><span class="muted">Ab tak jama (verified)</span><span class="paid">− ${formatINR(dues.paid)}</span></div>
+        ${dues.outstanding > 0
+          ? `<div class="r total"><span>Bakaya (Outstanding)</span><span class="owe">${formatINR(dues.outstanding)}</span></div>`
+          : `<div class="invoice-clear"><span class="tick">✓ FY ${escapeHtml(fy)} ka poora maintenance clear hai</span></div>`}
+      </div>
+    </div>` : `
+    <div class="invoice-card">
+      <div class="invoice-head"><span class="lbl">Maintenance Invoice</span><span class="fy">FY ${escapeHtml(fy)}</span></div>
+      <div class="invoice-rows"><div class="r"><span class="muted">Is saal ke liye rate abhi set nahi hua — koi bhi rakam de sakte hain.</span></div></div>
+    </div>`;
+
   const body = document.getElementById('paymentModalBody');
   body.innerHTML = `
+    ${invoiceHtml}
     <div class="pay-option" data-mode="cash"><svg viewBox="0 0 24 24"><use href="#i-cash"/></svg><div><b>Cash</b><div class="t-muted" style="font-size:12.5px;">Visit the RWA office</div></div></div>
     <div class="pay-option" data-mode="cheque"><svg viewBox="0 0 24 24"><use href="#i-cheque"/></svg><div><b>Cheque</b><div class="t-muted" style="font-size:12.5px;">Visit the RWA office</div></div></div>
     <div class="pay-option" data-mode="upi"><svg viewBox="0 0 24 24"><use href="#i-qr"/></svg><div><b>UPI / QR Scan</b><div class="t-muted" style="font-size:12.5px;">Pay instantly from your phone</div></div></div>
@@ -1674,12 +2128,12 @@
     opt.addEventListener('click', () => {
       body.querySelectorAll('.pay-option').forEach(o => o.classList.remove('selected'));
       opt.classList.add('selected');
-      renderPayDetailForm(opt.dataset.mode, paySettings);
+      renderPayDetailForm(opt.dataset.mode, paySettings, dues, hasRate);
     });
   });
 }
 
-function renderPayDetailForm(mode, paySettings) {
+function renderPayDetailForm(mode, paySettings, dues, hasRate) {
   const holder = document.getElementById('payDetailForm');
   const isOffline = mode === 'cash' || mode === 'cheque';
   holder.innerHTML = `
@@ -1693,18 +2147,53 @@
           <div style="font-size:12.4px;" class="t-muted">${escapeHtml(paySettings.bankName || '')} · A/C ${escapeHtml(paySettings.accountNumber || '—')} · IFSC ${escapeHtml(paySettings.ifsc || '—')}</div>
         </div>
       `}
-      <div class="field"><label for="payAmount">Amount (₹)</label>
-        <input type="number" id="payAmount" min="1" required inputmode="numeric"
-               value="${currentDues && currentDues.outstanding > 0 ? currentDues.outstanding : ''}">
-        ${currentDues && currentDues.outstanding > 0
-          ? `<div class="hint">Aapka balance ${formatINR(currentDues.outstanding)} hai. Isse kam bhi de sakte hain — baaki rakam bakaya dikhti rahegi.</div>`
-          : ''}
+      <div class="amount-block">
+        ${dues.outstanding > 0 ? `
+          <label style="display:block; font-size:11px; font-family:var(--font-mono); letter-spacing:.06em; text-transform:uppercase; color:var(--ink-400); margin-bottom:8px;">Kitna pay karna hai?</label>
+          <div class="amount-presets">
+            <button type="button" class="amount-preset selected" data-preset="full" data-amt="${dues.outstanding}">
+              <div class="pa-top">Poora bakaya</div>
+              <div class="pa-amt">${formatINR(dues.outstanding)}</div>
+            </button>
+            <button type="button" class="amount-preset" data-preset="custom">
+              <div class="pa-top">Alag rakam</div>
+              <div class="pa-amt">Choose</div>
+            </button>
+          </div>
+        ` : ''}
+        <div class="field" id="amountFieldWrap"${dues.outstanding > 0 ? ' style="display:none;"' : ''}>
+          <label for="payAmount">Amount (₹)</label>
+          <input type="number" id="payAmount" min="1" required inputmode="numeric"
+                 value="${dues.outstanding > 0 ? dues.outstanding : ''}">
+          ${dues.outstanding > 0
+            ? `<div class="hint">Poore bakaye se kam bhi de sakte hain — baaki rakam outstanding dikhti rahegi.</div>`
+            : (hasRate ? `<div class="hint">Is saal ka maintenance clear hai. Aap advance ya koi aur payment de sakte hain.</div>` : '')}
+        </div>
       </div>
       ${!isOffline ? `<div class="field"><label for="payUtr">UTR / Transaction ID</label><input id="payUtr" required autocapitalize="characters"></div>
       <div class="field"><label for="payProof">Payment Screenshot <span class="t-muted" style="font-weight:400;">(optional)</span></label><input type="file" id="payProof" accept="image/*"></div>` : ''}
       <button class="btn btn-primary btn-block" id="submitPaymentBtn">${isOffline ? 'Submit — I Will Pay at Office' : 'I Have Paid'}</button>
     </div>
   `;
+  // Preset toggle: "Poora bakaya" locks the amount to the outstanding figure;
+  // "Alag rakam" reveals the editable field. Locking is the default so the
+  // common case — pay exactly what you owe — is one tap with no typing.
+  const presets = holder.querySelectorAll('.amount-preset');
+  const amtWrap = document.getElementById('amountFieldWrap');
+  const amtInput = document.getElementById('payAmount');
+  presets.forEach(btn => btn.addEventListener('click', () => {
+    presets.forEach(b => b.classList.remove('selected'));
+    btn.classList.add('selected');
+    if (btn.dataset.preset === 'full') {
+      amtInput.value = btn.dataset.amt;
+      amtWrap.style.display = 'none';           // locked to the outstanding
+    } else {
+      amtWrap.style.display = 'block';          // let them type a different sum
+      amtInput.value = '';
+      amtInput.focus();
+    }
+  }));
+
   window.__translate(holder);
   document.getElementById('submitPaymentBtn').addEventListener('click', () => submitPayment(mode));
 }
@@ -1757,6 +2246,8 @@
 
 /* ---- Public content ---- */
 async function loadNotices() {
+  const _nl = document.getElementById('noticeList');
+  if (_nl && !_nl.dataset.loaded) _nl.innerHTML = skeletonList(2);
   const listEl = document.getElementById('noticeList');
   try {
     const q = query(collection(db, 'notices'), orderBy('postedAt', 'desc'), limit(5));
@@ -1764,18 +2255,19 @@
     renderNotices();
   } catch (e) {
     window.__notices = null;
-    listEl.innerHTML = '<div class="empty" style="padding:30px 12px;"><p>Notices could not be loaded.</p></div>';
+    listEl.innerHTML = emptyState('Notices load nahi ho paaye', 'Internet check karke page refresh karein.', { compact: true });
     window.__translate(listEl);
   }
 }
 function renderNotices() {
   const listEl = document.getElementById('noticeList');
+  listEl.dataset.loaded = '1';
   const rows = window.__notices;
 
   // Guard: without this, switching language on a society with no notices
   // yet would blank the box instead of keeping the empty-state message.
   if (!rows || !rows.length) {
-    listEl.innerHTML = '<div class="empty" style="padding:30px 12px;"><p>No notices have been posted yet.</p></div>';
+    listEl.innerHTML = emptyState('Abhi koi notice nahi', 'Committee ke naye notices yahan dikhenge.', { compact: true });
     window.__translate(listEl);
     return;
   }
@@ -1792,12 +2284,31 @@
           <h4 data-no-translate>${escapeHtml(lang === 'hi' ? (n.titleHi || n.titleEn) : n.titleEn)}</h4>
           <p data-no-translate>${escapeHtml(lang === 'hi' ? (n.bodyHi || n.bodyEn) : n.bodyEn)}</p>
           ${n.attachmentURL ? `<a href="${n.attachmentURL}" target="_blank" rel="noopener" style="font-size:12.5px; text-decoration:underline; display:inline-block; margin-top:7px;" class="t-sub">Download attachment</a>` : ''}
+          <div class="notice-actions">
+            <a class="notice-share" target="_blank" rel="noopener"
+               href="${waShareText(noticeShareText(n, lang))}"
+               aria-label="Share this notice on WhatsApp">
+              <svg viewBox="0 0 24 24" width="15" height="15" aria-hidden="true"><use href="#i-whatsapp"/></svg>
+              WhatsApp
+            </a>
+          </div>
         </div>
       </div>`;
   }).join('');
   window.__translate(listEl);
 }
 
+/* Plain-text version of a notice for a WhatsApp message: title, body, the
+   society name, and the attachment link if there is one. Uses whichever
+   language the portal is currently in. */
+function noticeShareText(n, lang) {
+  const title = lang === 'hi' ? (n.titleHi || n.titleEn) : n.titleEn;
+  const body = lang === 'hi' ? (n.bodyHi || n.bodyEn) : n.bodyEn;
+  const lines = [`📢 ${title}`, '', body, '', '— Max Heights Majestic RWS'];
+  if (n.attachmentURL) lines.push('', n.attachmentURL);
+  return lines.join('\n');
+}
+
 // Re-render notices (and re-translate the page) whenever the language changes.
 window.addEventListener('mhm:langchange', () => {
   if (document.getElementById('noticeList')) renderNotices();
@@ -1844,7 +2355,7 @@
     `;
     window.__translate(el);
   } catch (e) {
-    el.innerHTML = '<div class="empty" style="padding:38px 12px;"><p>Accounts could not be loaded.</p></div>';
+    el.innerHTML = emptyState('Accounts load nahi ho paaye', 'Thodi der baad dobara try karein.', { compact: true });
     window.__translate(el);
   }
 }
@@ -1855,7 +2366,7 @@
     const rows = (await getDocs(query(collection(db, 'documents'), orderBy('addedAt', 'desc'))))
       .docs.map(d => d.data());
     if (!rows.length) {
-      el.innerHTML = '<div class="empty" style="padding:34px 12px;"><p>No documents have been published yet.</p></div>';
+      el.innerHTML = emptyState('Abhi koi document nahi', 'Bylaws, minutes aur forms yahan publish honge.', { compact: true });
       window.__translate(el); return;
     }
     el.innerHTML = '<div class="doc-grid">' + rows.map(d => `
@@ -1869,7 +2380,7 @@
       </a>`).join('') + '</div>';
     window.__translate(el);
   } catch (e) {
-    el.innerHTML = '<div class="empty" style="padding:34px 12px;"><p>Documents could not be loaded.</p></div>';
+    el.innerHTML = emptyState('Documents load nahi ho paaye', 'Thodi der baad dobara try karein.', { compact: true });
     window.__translate(el);
   }
 }
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/logo-data.js MHMRWS-Portal/logo-data.js
--- a/MHMRWS-Portal/logo-data.js	2026-07-23 04:00:32.000000000 +0000
+++ MHMRWS-Portal/logo-data.js	2026-07-25 12:40:22.127415688 +0000
@@ -1,10 +1,46 @@
 /* ==========================================================================
-   MHMRWS society seal — embedded directly as a base64 WebP data URI.
+   MHMRWS society seal
+   --------------------------------------------------------------------------
+   The seal now lives in logo.webp as a real image file, not as 66 KB of
+   base64 inlined here. That base64 used to parse on EVERY page before first
+   paint, in a render-blocking module — pure dead weight for the common case,
+   where the seal is just an <img> the browser can fetch and cache on its own.
 
-   WHY EMBEDDED: the logo must never depend on a separate file upload
-   succeeding. Even if an image file is missing or was not deployed, the
-   seal still renders everywhere. The icon-*.png files at the repo root are
-   kept only for the PWA manifest and favicon, which need real files.
+   What still needs a data-URI is the PDF and canvas code (jsPDF cannot take a
+   URL, and a <canvas> tainted by a cross-origin-ish load cannot be exported).
+   So this module keeps a data-URI available — but fetches it lazily, only when
+   something actually asks, and caches the result. On a normal visit it never
+   runs at all.
+
+   This module still imports nothing from Firebase, so the branding path stays
+   independent of the SDK: an <img src="logo.webp"> renders even if everything
+   else on the page fails to load.
    ========================================================================== */
 
-export const LOGO_DATA_URI = 'data:image/webp;base64,';
+// Resolve logo.webp relative to this module, so it works from / and from /tools.
+export const LOGO_URL = new URL('./logo.webp', import.meta.url).href;
+
+let _dataUriPromise = null;
+
+/* Fetch the seal and return it as a data: URI, for jsPDF / canvas. Cached, so
+   repeated receipts in one session cost one network read (and the service
+   worker serves it offline). */
+export function getLogoDataUri() {
+  if (_dataUriPromise) return _dataUriPromise;
+  _dataUriPromise = fetch(LOGO_URL)
+    .then(r => { if (!r.ok) throw new Error('logo fetch ' + r.status); return r.blob(); })
+    .then(blob => new Promise((res, rej) => {
+      const fr = new FileReader();
+      fr.onload = () => res(fr.result);
+      fr.onerror = () => rej(fr.error);
+      fr.readAsDataURL(blob);
+    }))
+    .catch(err => { _dataUriPromise = null; throw err; });  // let a later call retry
+  return _dataUriPromise;
+}
+
+/* Back-compat shim. Older code imported LOGO_DATA_URI expecting a ready string.
+   That eager value is exactly what we are trying to avoid, so this is now the
+   image URL — correct for any <img src> or CSS use. Code paths that feed jsPDF
+   or canvas must call getLogoDataUri() instead; those have been updated. */
+export const LOGO_DATA_URI = LOGO_URL;
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/storage.rules MHMRWS-Portal/storage.rules
--- a/MHMRWS-Portal/storage.rules	2026-07-23 11:10:24.000000000 +0000
+++ MHMRWS-Portal/storage.rules	2026-07-24 11:52:24.731504335 +0000
@@ -22,6 +22,21 @@
         firestore.exists(/databases/(default)/documents/admins/$(request.auth.uid));
     }
 
+    // An upload path that accepts "any bytes" is a hosting service for whoever
+    // finds it: an .html file dropped into the bucket is served from the
+    // society's own domain and can then be linked as if it were an official
+    // document. Everything a resident uploads is either a picture or a PDF —
+    // or, for the ID vault, an AES-GCM blob, which arrives as
+    // application/octet-stream because that is what encryptFile() produces.
+    function isImageOrPdf() {
+      return request.resource.contentType.matches('image/.*')
+          || request.resource.contentType == 'application/pdf';
+    }
+    function isImagePdfOrEncrypted() {
+      return isImageOrPdf()
+          || request.resource.contentType == 'application/octet-stream';
+    }
+
     // Resident registration photo — owner + admins only (not public).
     match /photos/{uid}/{fileName} {
       allow read: if isAdmin() || request.auth.uid == uid;
@@ -35,25 +50,33 @@
     // before upload (see app-common.js encryptFile()) for defense in depth.
     match /documents/{uid}/{fileName} {
       allow read: if isAdmin() || request.auth.uid == uid;
-      allow write: if request.auth.uid == uid && request.resource.size < 8 * 1024 * 1024;
+      allow write: if request.auth.uid == uid
+                   && request.resource.size < 8 * 1024 * 1024
+                   && isImagePdfOrEncrypted();
     }
 
     // UPI/bank-transfer payment screenshots — owner + admins only.
     match /paymentProofs/{uid}/{fileName} {
       allow read: if isAdmin() || request.auth.uid == uid;
-      allow write: if request.auth.uid == uid && request.resource.size < 8 * 1024 * 1024;
+      allow write: if request.auth.uid == uid
+                   && request.resource.size < 8 * 1024 * 1024
+                   && isImageOrPdf();
     }
 
     // Notice board attachments (circulars, PDFs) — public read, admin write.
     match /noticeAttachments/{fileName} {
       allow read: if true;
-      allow write: if isAdmin();
+      allow write: if isAdmin()
+                   && request.resource.size < 20 * 1024 * 1024
+                   && isImageOrPdf();
     }
 
     // Committee directory photos — intentionally public.
     match /committeePhotos/{fileName} {
       allow read: if true;
-      allow write: if isAdmin();
+      allow write: if isAdmin()
+                   && request.resource.size < 5 * 1024 * 1024
+                   && request.resource.contentType.matches('image/.*');
     }
 
     // Society documents residents can download — bylaws, AGM minutes,
@@ -67,23 +90,41 @@
     // that turns into a leak the next time someone edits them.
     match /publicDocs/{fileName} {
       allow read: if true;
-      allow write: if isAdmin() && request.resource.size < 20 * 1024 * 1024;
+      allow write: if isAdmin()
+                   && request.resource.size < 20 * 1024 * 1024
+                   && isImageOrPdf();
     }
 
     // Society logo, payment QR code, downloadable forms — public read.
     match /settings/{fileName} {
       allow read: if true;
-      allow write: if isAdmin();
+      allow write: if isAdmin()
+                   && request.resource.size < 10 * 1024 * 1024
+                   && isImageOrPdf();
     }
   }
 }
 
 // ----------------------------------------------------------------------------
-// FALLBACK if firestore.exists() is not available on your project:
-// Replace isAdmin() above with `return isSignedIn();` and rely on the fact
-// that admin accounts are never self-service (only a Super Admin can create
-// one from the Admin Panel — see SETUP-GUIDE.md, Step 6). This is weaker
-// (any signed-in resident could read any resident's documents) so upgrade
-// to the firestore.exists() version above as soon as you can confirm it
-// works on your project.
+// IF firestore.exists() IS NOT AVAILABLE ON YOUR PROJECT
+//
+// Do NOT fall back to `return isSignedIn();` here. That would make every
+// resident's Aadhaar and PAN upload readable by every other resident who can
+// log in — which is the single worst outcome this file exists to prevent, and
+// it fails silently: nothing breaks, nothing warns you, the documents are just
+// exposed.
+//
+// If the Firestore lookup genuinely does not work, mirror the admin UIDs into
+// this file by hand instead. It is ugly and you have to edit it whenever the
+// committee changes, but it fails closed:
+//
+//   function isAdmin() {
+//     return isSignedIn() && request.auth.uid in [
+//       'PASTE_SUPERADMIN_UID_HERE',
+//       'PASTE_TREASURER_UID_HERE'
+//     ];
+//   }
+//
+// Firestore-in-Storage-rules has been generally available since 2023, so in
+// practice the version above should just work.
 // ----------------------------------------------------------------------------
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/styles.css MHMRWS-Portal/styles.css
--- a/MHMRWS-Portal/styles.css	2026-07-23 03:47:54.000000000 +0000
+++ MHMRWS-Portal/styles.css	2026-07-25 15:50:39.292853932 +0000
@@ -233,10 +233,20 @@
   text-align:left; font-family:var(--font-mono); font-size:11px; letter-spacing:.08em; text-transform:uppercase;
   color:var(--ink-400); padding:12px 14px; border-bottom:1px solid var(--line); background:var(--paper); white-space:nowrap;
 }
-tbody td{ padding:12px 14px; border-bottom:1px solid var(--line); color:var(--ink-700); vertical-align:middle; }
+/* Proportional digits make a column of rupee figures unreadable: 1 is narrow,
+   4 and 7 are wide, so 1,400 and 2,400 do not line up and a defaulters list
+   reads as mush. Tabular figures give every digit the same advance width, so
+   the decimal points stack the way they would on a printed ledger. IBM Plex
+   Sans ships the feature; this is the whole fix. */
+tbody td{ padding:12px 14px; border-bottom:1px solid var(--line); color:var(--ink-700); vertical-align:middle;
+          font-variant-numeric:tabular-nums; }
 tbody tr:last-child td{ border-bottom:none; }
 tbody tr:hover{ background:#FAFBFD; }
-.mono{ font-family:var(--font-mono); font-size:12.5px; letter-spacing:.01em; }
+.mono{ font-family:var(--font-mono); font-size:12.5px; letter-spacing:.01em; font-variant-numeric:tabular-nums; }
+/* Any figure meant to be compared against another figure above or below it. */
+.tnum, .stat-strip .stat b, .badge, .print-receipt .pr-amount .num, .print-receipt .pr-grid{
+  font-variant-numeric:tabular-nums;
+}
 
 /* --- Responsive tables ------------------------------------------------------
    Add class "responsive" to a .table-wrap and give every <td> a data-label.
@@ -286,12 +296,45 @@
   position:fixed; inset:0; background:rgba(10,20,40,.55); backdrop-filter:blur(2px);
   display:none; align-items:center; justify-content:center; padding:20px; z-index:80;
 }
-.modal-backdrop.open{ display:flex; }
+.modal-backdrop.open{ display:flex; animation:modal-veil .18s ease both; }
 .modal{ background:var(--white); border-radius:var(--radius-lg); max-width:560px; width:100%; max-height:88vh; overflow-y:auto; box-shadow:var(--shadow-pop); }
+
+/* The backdrop is toggled by adding/removing .open, which flips `display` —
+   and display is not animatable, so the modal used to appear in a single
+   frame. Animating on the .open class instead costs no JavaScript change and
+   gives the payment-verification dialog, the most-used control in the admin
+   panel, somewhere to come from.
+
+   Entry only. There is no exit animation because .open is removed
+   synchronously and the element is gone before a transition could run; doing
+   that properly needs the <dialog> element (see the note below). An instant
+   dismissal reads as responsive anyway — it is the arrival that needs to be
+   explained to the eye, not the departure. */
+.modal-backdrop.open .modal{ animation:modal-rise .24s cubic-bezier(.2,.7,.3,1) both; }
+@keyframes modal-veil{ from{ opacity:0; } }
+@keyframes modal-rise{ from{ opacity:0; transform:translateY(14px) scale(.985); } }
+
+/* On a phone a sheet rising from the bottom edge matches where the thumb
+   already is, and matches what every native app on the device does. */
+@media (max-width: 640px){
+  .modal-backdrop{ align-items:flex-end; padding:0; }
+  .modal{ max-width:none; border-radius:var(--radius-lg) var(--radius-lg) 0 0; max-height:92vh;
+          padding-bottom:env(safe-area-inset-bottom); }
+  .modal-backdrop.open .modal{ animation:modal-sheet .26s cubic-bezier(.2,.7,.3,1) both; }
+  @keyframes modal-sheet{ from{ transform:translateY(100%); } }
+}
+
+/* NOTE for later: replacing .modal-backdrop with a real <dialog> and
+   showModal() would give focus trapping, Esc-to-close, an inert background and
+   animatable exits, all for free. It is a markup change across six dialogs in
+   admin.html, so it is not done here — but it is the right end state. */
 .modal-head{ display:flex; align-items:center; justify-content:space-between; padding:20px 24px; border-bottom:1px solid var(--line); }
 .modal-body{ padding:24px; }
 
 /* --- Toast ------------------------------------------------------------------ */
+/* Pages that have a fixed bottom bar raise this so toasts clear it. */
+:root{ --toast-bottom-gap:16px; }
+
 #toast-region{ position:fixed; top:18px; right:18px; z-index:100; display:flex; flex-direction:column; gap:10px; max-width:340px; }
 .toast{
   background:var(--navy-950); color:var(--white); padding:13px 16px; border-radius:var(--radius-md);
@@ -300,7 +343,24 @@
 }
 .toast.success{ background:var(--success); }
 .toast.error{ background:var(--danger); }
+.toast.leaving{ animation:toast-out .3s ease forwards; }
 @keyframes toast-in{ from{ opacity:0; transform:translateY(-8px);} to{ opacity:1; transform:translateY(0);} }
+@keyframes toast-out{ to{ opacity:0; transform:translateY(-6px); } }
+
+/* Top-right is the wrong corner on a phone. It sits under the sticky header,
+   and it is the furthest point on the screen from the thumb — so a toast that
+   says "verify your email first" appears exactly where nobody is looking.
+   Anchored to the bottom instead, clear of the tab bar, full width. */
+@media (max-width: 900px){
+  #toast-region{
+    top:auto; bottom:calc(var(--toast-bottom-gap) + env(safe-area-inset-bottom));
+    left:14px; right:14px; max-width:none;
+  }
+  .toast{ font-size:14px; padding:14px 16px; animation:toast-in-up .25s ease; }
+  .toast.leaving{ animation:toast-out-down .3s ease forwards; }
+  @keyframes toast-in-up{ from{ opacity:0; transform:translateY(10px);} to{ opacity:1; transform:translateY(0);} }
+  @keyframes toast-out-down{ to{ opacity:0; transform:translateY(8px); } }
+}
 
 /* --- Loader ------------------------------------------------------------------ */
 .spinner{ width:20px; height:20px; border-radius:50%; border:2.5px solid rgba(255,255,255,.35); border-top-color:currentColor; animation:spin .7s linear infinite; }
@@ -308,9 +368,124 @@
 .skeleton{ background:linear-gradient(90deg, var(--line) 25%, #EDF0F6 37%, var(--line) 63%); background-size:400% 100%; animation:sk 1.4s ease infinite; border-radius:6px; }
 @keyframes sk{ 0%{background-position:100% 50%;} 100%{background-position:0 50%;} }
 
+/* Reusable skeleton rows for lists and tables. Instead of a bare "Loading…"
+   that then jumps to content, every list shows a few grey rows shaped like the
+   real thing, so the arrival is a fade rather than a jolt. Build them with the
+   skeletonRows()/skeletonList() helpers in app-common.js. */
+.sk-row{ display:flex; align-items:center; gap:14px; padding:14px 4px; }
+.sk-line{ height:12px; border-radius:6px; }
+.sk-line.sk{ background:linear-gradient(90deg, var(--line) 25%, #EDF0F6 37%, var(--line) 63%);
+  background-size:400% 100%; animation:sk 1.4s ease infinite; }
+.sk-w-40{ width:40%; } .sk-w-60{ width:60%; } .sk-w-25{ width:25%; } .sk-w-80{ width:80%; }
+.sk-stack{ display:flex; flex-direction:column; gap:8px; flex:1; }
+.sk-pill{ width:74px; height:26px; border-radius:var(--radius-pill); flex-shrink:0; }
+@media (prefers-reduced-motion: reduce){ .skeleton, .sk-line.sk{ animation:none; } }
+
 /* --- Empty state -------------------------------------------------------------- */
 .empty{ text-align:center; padding:48px 20px; color:var(--ink-400); }
 .empty .seal{ margin:0 auto 16px; }
+.empty h4{ color:var(--ink-600); font-size:15px; margin-bottom:6px; }
+.empty p{ font-size:13.5px; max-width:340px; margin:0 auto; }
+/* Compact variant for inside a table cell or a short panel. */
+.empty.empty-sm{ padding:30px 16px; }
+
+/* --- Inline field errors ------------------------------------------------------ */
+/* A validation problem belongs under the field that caused it, not in a toast
+   at the corner of the screen the user then has to hunt back from. */
+.field.has-error input, .field.has-error select, .field.has-error textarea{
+  border-color:var(--danger); background:#FEF4F3;
+}
+.field-error{ display:none; color:var(--danger); font-size:12px; margin-top:5px; font-weight:500; }
+.field.has-error .field-error{ display:block; }
+
+/* --- Maintenance invoice (payment modal) -------------------------------------- */
+/* Turns "type an amount" into "here is exactly what you owe, tap to pay it".
+   The committee's per-tower rate already produces expected/paid/outstanding
+   (duesFor); this just shows that breakdown as a bill and pre-locks the amount
+   to the outstanding figure, so a resident pays the right sum without doing
+   arithmetic — the whole point of the feature. */
+.invoice-card{ border:1px solid var(--line); border-radius:var(--radius-md); overflow:hidden; margin-bottom:16px; }
+.invoice-head{ background:var(--navy-950); color:#fff; padding:12px 14px; display:flex; align-items:center; justify-content:space-between; }
+.invoice-head .lbl{ font-family:var(--font-mono); font-size:10.5px; letter-spacing:.1em; text-transform:uppercase; color:var(--gold-500); }
+.invoice-head .fy{ font-size:12px; color:rgba(255,255,255,.75); }
+.invoice-rows{ padding:6px 14px; }
+.invoice-rows .r{ display:flex; justify-content:space-between; padding:8px 0; font-size:13.5px; border-bottom:1px solid var(--line); font-variant-numeric:tabular-nums; }
+.invoice-rows .r:last-child{ border-bottom:none; }
+.invoice-rows .r.total{ font-weight:700; color:var(--navy-950); font-size:15px; }
+.invoice-rows .r .muted{ color:var(--ink-400); }
+.invoice-rows .r .paid{ color:var(--success); }
+.invoice-rows .r .owe{ color:var(--danger); font-weight:700; }
+.invoice-clear{ padding:16px 14px; text-align:center; }
+.invoice-clear .tick{ color:var(--success); font-size:15px; font-weight:600; }
+/* Amount presets */
+.amount-presets{ display:flex; gap:8px; margin-bottom:10px; flex-wrap:wrap; }
+.amount-preset{ flex:1; min-width:120px; padding:11px 12px; border:1.5px solid var(--line); border-radius:var(--radius-md);
+  background:var(--white); cursor:pointer; text-align:left; transition:border-color .15s ease, background .15s ease; }
+.amount-preset.selected{ border-color:var(--saffron-500); background:var(--paper); }
+.amount-preset .pa-top{ font-size:11px; color:var(--ink-400); font-family:var(--font-mono); letter-spacing:.04em; text-transform:uppercase; }
+.amount-preset .pa-amt{ font-size:16px; font-weight:700; color:var(--navy-950); font-variant-numeric:tabular-nums; margin-top:2px; }
+.amount-preset.locked-note{ cursor:default; }
+
+/* --- Maintenance invoice (payment modal) end ---------------------------------- */
+
+/* --- Pager --------------------------------------------------------------------- */
+.pager{ display:flex; align-items:center; justify-content:space-between; gap:14px;
+  flex-wrap:wrap; margin-top:16px; }
+.pager:empty{ margin-top:0; }
+.pager-info{ font-family:var(--font-mono); font-size:12px; color:var(--ink-400); font-variant-numeric:tabular-nums; }
+.pager-btns{ display:flex; align-items:center; gap:4px; margin-left:auto; }
+.pager-btn{
+  min-width:34px; height:34px; padding:0 8px; border-radius:var(--radius-md);
+  border:1px solid var(--line); background:var(--white); color:var(--ink-700);
+  font-size:13.5px; font-weight:600; cursor:pointer; font-variant-numeric:tabular-nums;
+  transition:background .15s ease, border-color .15s ease;
+}
+.pager-btn:hover:not(:disabled):not(.is-current){ border-color:var(--gold-500); background:var(--paper); }
+.pager-btn.is-current{ background:var(--navy-950); color:#fff; border-color:transparent; cursor:default; }
+.pager-btn:disabled{ opacity:.4; cursor:not-allowed; }
+.pager-gap{ color:var(--ink-400); padding:0 2px; }
+@media (max-width:560px){ .pager{ justify-content:center; } .pager-btns{ margin-left:0; } }
+
+/* --- Registration steps ------------------------------------------------------- */
+/* The form has three natural sections; on a phone all fifteen fields in one
+   scroll is a wall. Showing one section at a time with a progress indicator
+   makes it feel short. Pure progressive enhancement: .steps-on is added by JS,
+   so if the script fails every fieldset simply shows and the form still works. */
+.step-indicator{ display:flex; align-items:center; gap:0; margin:0 0 22px; }
+.step-dot{ display:flex; flex-direction:column; align-items:center; gap:6px; flex:0 0 auto; }
+.step-dot .num{
+  width:30px; height:30px; border-radius:50%; display:grid; place-items:center;
+  font-family:var(--font-mono); font-size:13px; font-weight:600;
+  background:var(--paper); color:var(--ink-400); border:1.5px solid var(--line); transition:all .25s ease;
+}
+.step-dot .lbl{ font-size:10.5px; font-weight:600; letter-spacing:.03em; text-transform:uppercase; color:var(--ink-400); transition:color .25s ease; }
+.step-dot.active .num{ background:var(--gradient-saffron); color:#fff; border-color:transparent; }
+.step-dot.active .lbl{ color:var(--navy-950); }
+.step-dot.done .num{ background:var(--navy-950); color:#fff; border-color:transparent; }
+.step-line{ flex:1; height:2px; background:var(--line); margin:0 8px; align-self:flex-start; margin-top:14px; border-radius:2px; transition:background .25s ease; }
+.step-line.done{ background:var(--navy-950); }
+.steps-on fieldset{ display:none; }
+.steps-on fieldset.step-current{ display:block; animation:step-in .25s ease; }
+@keyframes step-in{ from{ opacity:0; transform:translateY(6px); } }
+.step-nav{ display:flex; gap:10px; margin-top:8px; }
+.step-nav .btn{ flex:1; }
+/* The declaration + submit belong to the last step only. */
+.steps-on .final-only{ display:none; }
+.steps-on.at-last .final-only{ display:block; }
+@media (prefers-reduced-motion: reduce){ .steps-on fieldset.step-current{ animation:none; } }
+
+/* --- Session expiry banner ---------------------------------------------------- */
+.session-banner{
+  position:fixed; left:0; right:0; bottom:0; z-index:120;
+  background:var(--navy-950); color:#fff; padding:14px 18px;
+  display:flex; align-items:center; gap:14px; flex-wrap:wrap; justify-content:center;
+  box-shadow:0 -6px 24px rgba(0,0,0,.18); font-size:13.5px;
+  transform:translateY(100%); animation:session-rise .3s ease forwards;
+}
+@keyframes session-rise{ to{ transform:translateY(0); } }
+.session-banner button{ padding:8px 16px; border-radius:var(--radius-md); font-weight:600; font-size:13px; cursor:pointer; border:none; }
+.session-banner .stay{ background:var(--gold-500); color:var(--navy-950); }
+.session-banner .out{ background:transparent; color:rgba(255,255,255,.7); border:1px solid rgba(255,255,255,.3); }
 
 /* --- Utility text colors -------------------------------------------------------- */
 .t-muted{ color:var(--ink-400); }
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/sw.js MHMRWS-Portal/sw.js
--- a/MHMRWS-Portal/sw.js	2026-07-23 10:40:52.000000000 +0000
+++ MHMRWS-Portal/sw.js	2026-07-25 12:49:36.553680996 +0000
@@ -8,7 +8,7 @@
    Bump CACHE_NAME on any future structural change to force a clean cache.
    ========================================================================== */
 
-const CACHE_NAME = 'mhmrws-shell-v4';
+const CACHE_NAME = 'mhmrws-shell-v6';
 const SHELL_FILES = [
   './',
   './index.html',
@@ -17,6 +17,7 @@
   './app-common.js',
   './manifest.json',
   './logo-data.js',
+  './logo.webp',
   './i18n.js',
   './tower-plan.js',
   './icon-192.png',
@@ -24,9 +25,17 @@
 ];
 
 self.addEventListener('install', (event) => {
+  // cache.addAll() is all-or-nothing: one 404 anywhere in SHELL_FILES and the
+  // whole install rejects, leaving the app with no offline fallback at all and
+  // nothing in the console to say why. Cache each file on its own instead, so
+  // a single missing asset costs exactly that one asset.
   event.waitUntil(
     caches.open(CACHE_NAME)
-      .then((cache) => cache.addAll(SHELL_FILES))
+      .then((cache) => Promise.all(
+        SHELL_FILES.map((url) =>
+          cache.add(url).catch((err) => console.warn('[sw] could not cache', url, err))
+        )
+      ))
       .then(() => self.skipWaiting())
   );
 });
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/tools/migrate-public-tokens.mjs MHMRWS-Portal/tools/migrate-public-tokens.mjs
--- a/MHMRWS-Portal/tools/migrate-public-tokens.mjs	1970-01-01 00:00:00.000000000 +0000
+++ MHMRWS-Portal/tools/migrate-public-tokens.mjs	2026-07-24 12:01:07.939416639 +0000
@@ -0,0 +1,196 @@
+#!/usr/bin/env node
+/* ==========================================================================
+   MHMRWS Portal — backfill public verification tokens
+   ==========================================================================
+   Members and payments created before the hardening release have no
+   `publicToken`, and their public verification documents are keyed by the
+   sequential Member ID / Receipt Number — which means they can be enumerated.
+   This script gives each one a random token and writes a token-keyed copy of
+   the public record.
+
+   It does NOT delete the old, sequentially-keyed documents unless you pass
+   --purge-legacy, because deleting them stops every receipt and membership
+   card already printed from verifying. See MIGRATION.md, section 4.
+
+   Usage:
+     npm install firebase-admin
+     node migrate-public-tokens.mjs --dry-run
+     node migrate-public-tokens.mjs
+     node migrate-public-tokens.mjs --purge-legacy
+
+   Requires serviceAccountKey.json in the parent directory. That key bypasses
+   security rules entirely — treat it like a password and delete it afterwards.
+   ========================================================================== */
+
+import { readFileSync } from 'node:fs';
+import { randomBytes } from 'node:crypto';
+import { initializeApp, cert } from 'firebase-admin/app';
+import { getFirestore, FieldValue } from 'firebase-admin/firestore';
+
+const DRY_RUN = process.argv.includes('--dry-run');
+const PURGE   = process.argv.includes('--purge-legacy');
+
+const KEY_PATH = new URL('../serviceAccountKey.json', import.meta.url);
+let serviceAccount;
+try {
+  serviceAccount = JSON.parse(readFileSync(KEY_PATH, 'utf8'));
+} catch (e) {
+  console.error('Could not read serviceAccountKey.json in the project root.');
+  console.error('Firebase Console -> Project settings -> Service accounts -> Generate new private key');
+  process.exit(1);
+}
+
+initializeApp({ credential: cert(serviceAccount) });
+const db = getFirestore();
+
+/* Must match newPublicToken() in app-common.js and isPublicToken() in
+   firestore.rules: 32 lowercase hex characters. */
+const newPublicToken = () => randomBytes(16).toString('hex');
+
+const say = (...a) => console.log(DRY_RUN ? '[dry-run]' : '[apply]  ', ...a);
+
+/* Firestore batches cap at 500 writes. */
+async function commitInChunks(writes) {
+  if (DRY_RUN) return writes.length;
+  let done = 0;
+  for (let i = 0; i < writes.length; i += 400) {
+    const batch = db.batch();
+    for (const w of writes.slice(i, i + 400)) w(batch);
+    await batch.commit();
+    done += Math.min(400, writes.length - i);
+    process.stdout.write(`\r  committed ${done}/${writes.length}`);
+  }
+  if (done) process.stdout.write('\n');
+  return done;
+}
+
+async function migrateMembers() {
+  console.log('\n=== members ===');
+  const snap = await db.collection('members').get();
+  const writes = [];
+  let skipped = 0;
+
+  for (const d of snap.docs) {
+    const m = d.data();
+    if (m.publicToken) { skipped++; continue; }
+    // Only approved members have a public record to move.
+    if (!m.memberID || m.status !== 'approved') { skipped++; continue; }
+
+    const token = newPublicToken();
+    say(`member ${m.memberID}  ${m.tower}/${m.flatNumber}  ->  ${token}`);
+
+    writes.push((b) => b.update(d.ref, { publicToken: token }));
+    writes.push((b) => b.set(db.collection('membersPublic').doc(token), {
+      memberID: m.memberID,
+      name: m.name || '',
+      tower: m.tower || '',
+      flatNumber: m.flatNumber || '',
+      residentType: m.residentType || '',
+      status: m.status,
+      updatedAt: FieldValue.serverTimestamp()
+    }));
+    if (PURGE) {
+      writes.push((b) => b.delete(db.collection('membersPublic').doc(m.memberID)));
+    }
+  }
+
+  const n = await commitInChunks(writes);
+  console.log(`  ${writes.length ? writes.length : 0} writes queued, ${n} applied, ${skipped} already fine or not applicable`);
+}
+
+async function migratePayments() {
+  console.log('\n=== payments ===');
+  const snap = await db.collection('payments').where('status', '==', 'verified').get();
+  const writes = [];
+  let skipped = 0;
+
+  for (const d of snap.docs) {
+    const p = d.data();
+    if (p.publicToken) { skipped++; continue; }
+    if (!p.receiptNumber) { skipped++; continue; }
+
+    const token = newPublicToken();
+    say(`receipt ${p.receiptNumber}  ${p.residentName}  ->  ${token}`);
+
+    writes.push((b) => b.update(d.ref, { publicToken: token }));
+    writes.push((b) => b.set(db.collection('receiptsPublic').doc(token), {
+      receiptNumber: p.receiptNumber,
+      residentName: p.residentName || '',
+      flatNumber: p.flatNumber || '',
+      tower: p.tower || '',
+      amount: p.amount,
+      financialYear: p.financialYear || '',
+      verifiedAt: p.verifiedAt || FieldValue.serverTimestamp(),
+      status: 'Verified'
+    }));
+    if (PURGE) {
+      writes.push((b) => b.delete(db.collection('receiptsPublic').doc(p.receiptNumber)));
+    }
+  }
+
+  const n = await commitInChunks(writes);
+  console.log(`  ${writes.length} writes queued, ${n} applied, ${skipped} already fine or not applicable`);
+}
+
+/* Approvals now reserve a flat. Existing approved members predate that, so
+   backfill the claims — and report any duplicate flats rather than guessing
+   which record is the real one. */
+async function backfillFlatClaims() {
+  console.log('\n=== flat claims ===');
+  const snap = await db.collection('members').where('status', '==', 'approved').get();
+  const byFlat = new Map();
+
+  for (const d of snap.docs) {
+    const m = d.data();
+    if (!m.tower || !m.flatNumber) continue;
+    const key = `${String(m.tower).trim()}_${String(m.flatNumber).trim()}`;
+    if (!byFlat.has(key)) byFlat.set(key, []);
+    byFlat.get(key).push({ id: d.id, ...m });
+  }
+
+  const writes = [];
+  const duplicates = [];
+  for (const [key, members] of byFlat) {
+    if (members.length > 1) {
+      duplicates.push({ key, members });
+      continue;   // never auto-pick a winner
+    }
+    const m = members[0];
+    say(`claim ${key} -> ${m.memberID || m.id}`);
+    writes.push((b) => b.set(db.collection('flatClaims').doc(key), {
+      tower: m.tower, flatNumber: m.flatNumber,
+      memberDocId: m.id, memberUid: m.uid || null,
+      memberID: m.memberID || null,
+      claimedAt: FieldValue.serverTimestamp()
+    }, { merge: true }));
+  }
+
+  const n = await commitInChunks(writes);
+  console.log(`  ${writes.length} claims queued, ${n} applied`);
+
+  if (duplicates.length) {
+    console.log(`\n  !! ${duplicates.length} flats have more than one approved member.`);
+    console.log('     No claim was written for these — decide which record is correct,');
+    console.log('     deactivate the other in the Admin Panel, then re-run this script.\n');
+    for (const { key, members } of duplicates) {
+      console.log(`     ${key}:`);
+      for (const m of members) console.log(`       - ${m.memberID || '(no ID)'}  ${m.name}  (doc ${m.id})`);
+    }
+  }
+}
+
+console.log(DRY_RUN ? 'DRY RUN — nothing will be written.' : 'APPLYING CHANGES.');
+if (PURGE && !DRY_RUN) {
+  console.log('\n!! --purge-legacy is on. Receipts and membership cards printed before');
+  console.log('   this migration will STOP verifying. Ctrl-C now if that is not what');
+  console.log('   you meant. Continuing in 10 seconds...\n');
+  await new Promise((r) => setTimeout(r, 10000));
+}
+
+await migrateMembers();
+await migratePayments();
+await backfillFlatClaims();
+
+console.log('\nDone.');
+if (DRY_RUN) console.log('That was a dry run — re-run without --dry-run to apply.');
+process.exit(0);
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/tools/verify-email-test.html MHMRWS-Portal/tools/verify-email-test.html
--- a/MHMRWS-Portal/tools/verify-email-test.html	1970-01-01 00:00:00.000000000 +0000
+++ MHMRWS-Portal/tools/verify-email-test.html	2026-07-25 11:44:33.536739649 +0000
@@ -0,0 +1,104 @@
+<!DOCTYPE html>
+<html lang="en">
+<head>
+<meta charset="UTF-8">
+<meta name="viewport" content="width=device-width, initial-scale=1">
+<title>Email Verification — Diagnostic</title>
+<!--
+  EMAIL VERIFICATION DIAGNOSTIC
+  =============================
+  Verification mail nahi aa raha? Yeh page asli Firebase error screen par dikha
+  deta hai, taaki guess na karna pade.
+
+  ISTEMAL KAISE KAREIN:
+    1. Is file ko project root mein rakhein (firebase-config.js ke saath), ya
+       path theek kar dein neeche import mein.
+    2. Live site par kholein — https://<your-domain>/tools/verify-email-test.html
+       (localhost par nahi; asli domain ka behaviour test karna hai).
+    3. Ek TEST email + password daalein (naya ya maujooda test account).
+    4. "Register + send" ya "Sign in + send" dabaayein.
+    5. Screen par jo result/error code aaye, woh neeche table se match karein.
+
+  Kaam ho jaane par is file ko site se HATA dein — yeh sirf diagnosis ke liye hai.
+-->
+<style>
+  body{font-family:system-ui,-apple-system,sans-serif;max-width:560px;margin:40px auto;padding:0 20px;color:#0A1B33;line-height:1.5}
+  h1{font-size:20px} h2{font-size:15px;margin-top:28px}
+  input,button{font-size:15px;padding:10px 12px;border-radius:8px;border:1px solid #ccc;width:100%;box-sizing:border-box;margin:6px 0}
+  button{background:#0A1B33;color:#fff;border:none;cursor:pointer;font-weight:600}
+  button.alt{background:#fff;color:#0A1B33;border:1px solid #0A1B33}
+  #out{white-space:pre-wrap;background:#f5f6fa;border:1px solid #e2e6ef;border-radius:8px;padding:14px;font-family:ui-monospace,monospace;font-size:13px;margin-top:16px;min-height:60px}
+  .ok{color:#1B7A4D;font-weight:700} .err{color:#B3261E;font-weight:700}
+  table{border-collapse:collapse;width:100%;font-size:13px;margin-top:8px}
+  td{border:1px solid #e2e6ef;padding:7px 9px;vertical-align:top}
+  code{background:#eef;padding:1px 5px;border-radius:4px}
+</style>
+</head>
+<body>
+<h1>Email verification — diagnostic</h1>
+<p>Yeh asli Firebase se verification mail bhejne ki koshish karta hai aur exact
+error code dikhata hai. Test account use karein.</p>
+
+<input id="email" type="email" placeholder="test email — e.g. yourname+test@gmail.com" autocomplete="off">
+<input id="pass" type="password" placeholder="password (min 6 chars)" autocomplete="new-password">
+
+<button id="reg">Register + send verification</button>
+<button id="signin" class="alt">Sign in + send verification (existing account)</button>
+
+<div id="out">Result yahan dikhega…</div>
+
+<h2>Error code → matlab</h2>
+<table>
+  <tr><td><code>(no error, success)</code></td><td>Mail bhej diya gaya. Ab <b>Spam / Promotions</b> folder dekhein — Gmail aksar wahin daalta hai. <code>firebaseapp</code> search karein.</td></tr>
+  <tr><td><code>auth/operation-not-allowed</code></td><td>Email/Password sign-in <b>disabled</b> hai. Console → Authentication → Sign-in method → enable karein.</td></tr>
+  <tr><td><code>auth/unauthorized-domain</code></td><td>Yeh domain <b>Authorized domains</b> mein nahi hai. Console → Authentication → Settings → Authorized domains → apna domain add karein.</td></tr>
+  <tr><td><code>auth/too-many-requests</code></td><td>Bahut baar bheja gaya, ya daily limit. 10–15 min rukein, phir try karein.</td></tr>
+  <tr><td><code>auth/invalid-email</code></td><td>Email ka format galat hai.</td></tr>
+  <tr><td><code>auth/email-already-in-use</code></td><td>Account pehle se hai — "Sign in + send" wala button use karein.</td></tr>
+  <tr><td><code>auth/api-key-not-valid</code></td><td>firebase-config.js mein API key galat hai ya restrict ki hui hai.</td></tr>
+</table>
+
+<script type="module">
+  // Path project root ke config par point karta hai. Agar aap ise root mein
+  // rakhein to './firebase-config.js' kar lein.
+  import { app } from '../firebase-config.js';
+  import {
+    getAuth, createUserWithEmailAndPassword, signInWithEmailAndPassword,
+    sendEmailVerification
+  } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
+
+  const auth = getAuth(app);
+  const out = document.getElementById('out');
+  const log = (msg, cls='') => { out.innerHTML = cls ? `<span class="${cls}">${msg}</span>` : msg; };
+
+  async function trySend(makeUser) {
+    const email = document.getElementById('email').value.trim();
+    const pass = document.getElementById('pass').value;
+    if (!email || pass.length < 6) { log('Email daalein aur password kam se kam 6 characters.', 'err'); return; }
+    log('Chal raha hai…');
+    try {
+      const cred = await makeUser(email, pass);
+      log('Account OK — ab verification mail bhej rahe hain…');
+      await sendEmailVerification(cred.user);
+      log(
+        'SUCCESS ✓ Firebase ne mail accept kar liya.\n\n' +
+        'Ab is inbox ka SPAM / PROMOTIONS folder dekhein — "firebaseapp" search karein.\n' +
+        'Agar 5 min mein bhi nahi aaya: sender ' + (auth.app.options.projectId || '<project>') +
+        '.firebaseapp.com blocked to nahi, ya Spark plan ki daily limit poori to nahi.',
+        'ok'
+      );
+    } catch (e) {
+      log('FAILED ✗\n\nerror.code:  ' + (e.code || '(none)') +
+          '\nerror.message: ' + (e.message || e) +
+          '\n\nUpar table se yeh code match karein.', 'err');
+      console.error(e);
+    }
+  }
+
+  document.getElementById('reg').onclick =
+    () => trySend((e,p) => createUserWithEmailAndPassword(auth, e, p));
+  document.getElementById('signin').onclick =
+    () => trySend((e,p) => signInWithEmailAndPassword(auth, e, p));
+</script>
+</body>
+</html>
diff -ruN '--exclude=*.png' '--exclude=*.webp' '--exclude=*.jpg' a/MHMRWS-Portal/verify.html MHMRWS-Portal/verify.html
--- a/MHMRWS-Portal/verify.html	2026-07-23 09:23:22.000000000 +0000
+++ MHMRWS-Portal/verify.html	2026-07-24 12:07:13.669361426 +0000
@@ -2,10 +2,48 @@
 <html lang="en">
 <head>
 <meta charset="UTF-8">
+<!-- ==========================================================================
+     CONTENT SECURITY POLICY
+     GitHub Pages cannot set HTTP headers, so this goes in a meta tag — which
+     must stay above every script and stylesheet it governs.
+
+     What this buys: an injected <script src="https://evil.example/x.js"> will
+     not load, and stolen data cannot be POSTed anywhere except the endpoints
+     listed in connect-src. That is the realistic attack against a page that
+     handles Aadhaar uploads and payment records.
+
+     What it does NOT buy: script-src still has 'unsafe-inline', because every
+     page here is built from inline module scripts and inline style attributes. Removing that needs the inline scripts moved into their
+     own .js files first — worth doing, but it is a refactor, not a patch.
+
+     frame-ancestors is not supported in a meta tag, so this cannot stop the
+     site being framed. If you ever move off GitHub Pages, set
+     `X-Frame-Options: DENY` (or frame-ancestors 'none') as a real header.
+
+     AFTER DEPLOYING: open the site, press F12, and check the Console for
+     "Refused to ..." messages. Any legitimate endpoint this policy blocks will
+     announce itself there and just needs adding to the right directive.
+     ========================================================================== -->
+<meta http-equiv="Content-Security-Policy" content="
+  default-src 'self';
+  script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://www.gstatic.com https://accounts.google.com https://apis.google.com https://www.google.com;
+  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
+  img-src 'self' data: blob: https://firebasestorage.googleapis.com https://*.googleusercontent.com;
+  font-src 'self' data: https://fonts.gstatic.com;
+  connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://accounts.google.com;
+  frame-src https://www.google.com https://accounts.google.com;
+  worker-src 'self';
+  object-src 'none';
+  base-uri 'self';
+  form-action 'self';
+">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Verify — MHMRWS</title>
 <meta name="robots" content="noindex">
 <link rel="icon" href="favicon-32.png">
+<meta name="color-scheme" content="light">
+<link rel="preconnect" href="https://fonts.googleapis.com">
+<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link rel="stylesheet" href="styles.css">
 <style>
   body{ background:var(--gradient-navy); min-height:100vh; display:flex; align-items:center; justify-content:center; padding:24px; }
@@ -43,9 +81,14 @@
 import { doc, getDoc } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
 import { escapeHtml, fmtDate, formatINR } from './app-common.js';
 
+/* The QR codes now carry an unguessable token (?r= / ?m=) instead of the
+   sequential receipt or member number, so the public records cannot be walked
+   from 000001 upwards. The old ?receipt= / ?member= parameters are still
+   honoured: receipts and cards printed before the change are already in
+   residents' hands and must keep verifying. */
 const params = new URLSearchParams(window.location.search);
-const receiptNumber = params.get('receipt');
-const memberID = params.get('member');
+const receiptKey = params.get('r') || params.get('receipt');
+const memberKey  = params.get('m') || params.get('member');
 
 const sealBox = document.getElementById('sealBox');
 const stateTitle = document.getElementById('stateTitle');
@@ -100,18 +143,18 @@
 }
 
 (async function run() {
-  if (!receiptNumber && !memberID) {
+  if (!receiptKey && !memberKey) {
     renderNotFound('Is link mein koi receipt ya member number nahi mila.');
     return;
   }
   try {
-    if (memberID) {
-      const snap = await getDoc(doc(db, 'membersPublic', memberID));
+    if (memberKey) {
+      const snap = await getDoc(doc(db, 'membersPublic', memberKey));
       if (snap.exists()) renderMember(snap.data());
-      else renderNotFound('Yeh Member ID hamare record mein nahi mila.');
+      else renderNotFound('Yeh membership hamare record mein nahi mili.');
       return;
     }
-    const snap = await getDoc(doc(db, 'receiptsPublic', receiptNumber));
+    const snap = await getDoc(doc(db, 'receiptsPublic', receiptKey));
     if (snap.exists()) renderVerified(snap.data());
     else renderNotFound();
   } catch (e) {
